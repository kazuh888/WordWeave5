# 移行後の作業順序と完了条件

## 0.5.1（2026-09-20）

最新の合否は[PATH探索検証](validation/path-search-0.5.1.md)を参照する。利用者は[設定手順](UPGRADE-0.5.1.md)で `codex` 自動検出または明示パスを選ぶ。探索成功はCLIの版や実生成成功とは別であり、0.153.4指定と手元の版の差を未解決のまま自動更新しない。0.5.0の音声等の実機確認は継続課題である。

## 0.5.0（2026-09-20）

1. [0.5.0検証記録](validation/controls-0.5.0.md) で現在の全テスト・releaseビルドの結果を確認する。変更後は同じコマンドを再実行し、旧版ログを引用しない。
2. [操作手順](UPGRADE-0.5.0.md) の実機確認を行う。実マイク・実音声・CLI0.153.4の候補取得／effort指定生成は自動試験と分ける。
3. 利用者の記録を退避した上で、会話削除・復元、媒体保持、再起動後のログと設定保存を確認する。

## 0.4.0の現在の優先順位（2026-09-17）

1. 最終ソースのWindows全119件テストとreleaseビルドは成功した。[今回の検証記録](validation/harness-0.4.0.md) で対象の指紋を確認する。これ以降ソースを変更した場合は再実行し、旧ログを代用しない。
2. [0.4.0の操作手順](UPGRADE-0.4.0.md) に沿って、少量の実Codex媒体入力・再取得とWindowsのマイク／ペン／読み上げを確認する。GUIのheadless試験は実操作評価とは別である。
3. 更新前の保存フォルダーを利用者が保全した上で、既存記録の読み込み、媒体付きバックアップ・復元と比較画面の使いやすさを確認する。

以下は0.3.8移行時の計画であり、テスト件数・未実装記述は当時のものである。現在の件数と残作業は上記記録および `tasks/todo.md` を正とする。

---

これは作業計画であり、合格済み一覧ではない。0.3.8のアプリコードは今回変更していない。

## P0：作業基準とバックアップ

- 手元のCargo.toml・ソースが0.3.8と一致するか確認し、追加のローカル変更を保全する。
- WordWeaveを終了し、学習データ一式をバックアップする。ソースの別フォルダーだけではデータは分離しない。
- OS、rustc/cargo、Codex CLIの版、実際の起動経路を記録する。認証情報本文を収集しない。
- 最新版への変更が未検証であることを作業記録に残す。

完了条件：採用したソースが特定され、既存データを戻せる。

## P1：ビルド・テスト

WindowsのCargo.tomlがあるフォルダーで実行する。

```powershell
cargo test --all-targets --locked
cargo build --release --locked --bin wordweave5
```

失敗したら、最初のコンパイルエラーまたは失敗テストに絞って修正する。根拠なくテストを無効化しない。通信の調査時には次を使用できる。

```powershell
cargo test --test codex_process --locked -- --test-threads=1
```

通信テストはWindowsで14件である。モックbinの0件表示と取り違えない。Linuxの成功だけではWindowsのGUI・Volta経路を確認できない。`validation/`の旧ログを新しい実行結果であるかのように引用しない。

完了条件：全対象テストとreleaseビルドが成功し、実際に実行した環境・コマンド・結果が記録される。環境の都合で実行できなければ、その範囲を未検証として残す。

## P2：実Codexと新しい画面

利用者のChatGPT利用枠を消費する確認は少量で行う。

| 確認 | 合格条件 |
| --- | --- |
| 起動・既存記録 | 既存教材と復習記録が読める。初期化されない |
| Codex起動 | 実Volta経路でinitialize、認証、生成が成立する |
| 実行設定 | 返却モデル・effortを今回／前回として表示。欠落値を推測しない |
| 初回・追加質問 | 前の回答に依存する質問へ文脈を踏まえて答える |
| 再起動 | 会話・下書き・メモ・回答時の実行設定が残る |
| 会話分離 | 別の会話へ前の会話を自動送信しない |
| 参照指定・長文 | 選んだ過去の往復を送信。対象外の履歴は保存に残り、プレビューが一致する |
| 中断・失敗 | 未完了回答を成功履歴に混ぜない。質問下書きを保持する |

完了条件：上記操作の結果が記録され、モック試験と実接続試験を分けて報告できる。

## P3：チャットからの教材化

実データを使う場合はバックアップ済みの少数教材で確認する。

1. 新しい基本語について2往復以上会話し、根拠とする往復だけ選ぶ。
2. 新規案を生成し、例文・訳・使用条件を編集する。登録前は教材に反映されないことを確認する。
3. 下書きのまま再起動し、編集内容が残ることを確認する。登録後に元の会話が開けることを確認する。
4. 既存教材へ「追加」を行い、既存説明・例文と復習状態を維持したまま新しい補足だけが入ることを確認する。
5. 同じ英文の説明変更は追加モードで上書きされず、訂正を案内することを確認する。
6. 訂正モードで基本項目を変更し、差分と復習状態更新の予告が一致することを確認する。
7. 同じ基本語の別用法、古い案との競合、変更なし、二重登録を検査する。
8. 教材と学習記録をエクスポートし、会話参照も含めて復元できることを確認する。教材のみのTSVでは参照は移行しない。

完了条件：新規／追加／訂正の動作が区別され、保存・再読み込みで教材と会話の関係を保つ。英文の意味・文法についても案を人が確認する。

## 既存機能の最小回帰確認

NGSLまたは提供語1語の生成→例文追加→日本文から英文生成→未処理からの再開を少量で確認する。5分コースの入力・復習記録、必要に応じてペン・読み上げ・録音再生も確認する。新機能の追加のために既存生成や学習を壊していないことを確認する。

## 記録テンプレート

```text
対象ソース／版：
実行日・OS・Rust・Codex CLI：
変更したファイルと理由：
実行したコマンドと結果：
実機で確認した操作と結果：
未検証の範囲：
残る問題と再現手順：
次の作業：
```
