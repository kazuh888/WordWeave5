# 0.6.0：既存機能を活かしたUX改善

Windows / eguiを維持し、学習の開始・回答・振り返りの流れを整理した。
保存形式、教材、復習規則、ChatGPT認証によるCodex接続は継続する。
画面イメージは情報の優先順位・青系の主操作・カード構成の参考とし、画像にしか存在しない機能や装飾は追加していない。

## 更新前

WordWeave5を閉じ、既存の保存フォルダーを保全する。録音・筆跡まで戻すには、設定の媒体付きバックアップか保存フォルダー全体のコピーを使う。学習記録JSONだけでは教材・媒体は含まれない。
開発テストは専用の一時フォルダーで行い、利用者の教材・学習記録には書き込んでいない。

## 主な操作

1. ホームの「今日の学習を始める」または学習タブから開始する。途中なら「学習を再開する」で入力を保持したまま戻る。
2. 語句・聞き取りはEnterで照合する。英作文・使い方の複数行入力はEnterで改行、Ctrl+Enterで照合する。IME変換確定Enterは照合しない。
3. 照合後は自分で評価を選ぶ。単にEnterを押しただけで「自力でできた」にはならない。Tabで評価ボタンを選び、Enterで確定できる。
4. 目安時間になっても回答途中では切らない。回答を評価した区切りで結果画面へ進む。「ここで終える」は未記録の入力がある場合に確認する。
5. 結果画面の「今日はここで終了」でホームへ戻る。「もう少し学習する」は既存の復習期限と新規日上限を維持する。出題対象がない場合は開始できない。

## 他の画面

- 教材：検索・一覧と内容表示を中心とし、生成・編集・学習対象変更は管理欄にまとめた。検索結果が0件なら無関係な教材を表示しない。
- 語彙を追加：共通生成設定、NGSL、入力した語、UTF-8ファイル、保存候補の入口を分けた。ファイル選択後に生成・登録が始まることを明記した。
- 英語チャット：送信を主操作にし、文脈・実行記録・削除は補助メニューへまとめた。通常幅ではドラッグ可能な左一覧、狭幅では会話選択メニューを使う。Enter改行／IME確定で改行なし／Ctrl+Enter送信を保持する。
- 教材に反映：選択した往復から案を作る。差分・理由・根拠を確認して初めて登録する。「追加」と「訂正」は区別する。
- 記録：日別学習時間と回答数を先に表示する。学習済みは回答経験がある語彙であり、習得完了ではない。ヒントなしGood/Easyも自己評価を含む。
- 設定：学習、音声、AI接続、生成上限、バックアップ、診断を区分した。接続確認は未確認／確認中／前回成功／前回失敗を区別し、実行ファイル変更後の成功を推測しない。

## 変更しない範囲

APIキーへのフォールバック、依存ライブラリーの更新、Android対応、出題方式追加、英語能力推定、Shift-JIS取込、汎用ファイル添付は含まない。録音・筆跡原本、音声操作、会話ごみ箱、実行結果の再取得、Voltaと保存PATHの探索は維持する。

## 実機受入（未検証のチェック項目）

- [ ] 保全済みの既存記録で起動し、教材・学習記録・会話・媒体が残っている。
- [ ] 5分の開始→キーボード回答→照合→明示評価→結果→終了／継続が分かりやすい。
- [ ] 日本語IMEの実変換、手書き、録音、TTS、音声認識の各操作が成立する。
- [ ] 最小820×650、文字倍率80%／100%／160%で文字・主ボタン・スクロールを操作できる。
- [ ] 実Codexの回答、返却モデル・effort、教材化の差分承認、失敗時の実行記録を確認できる。
- [ ] 終了・再起動後も記録と設定を読み戻せる。媒体付きバックアップを別の保全済み環境で復元できる。

自動テストや旧版の実機報告で上の項目を済ませた扱いにしない。今回の検証結果は [UX検証記録](validation/ux-0.6.0.md) を参照する。
