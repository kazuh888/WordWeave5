# WordWeave5 開発引き継ぎ

## 2026-09-21：0.6.0 UX改善

- 学習開始→入力→照合→明示評価→終了結果→終了／継続を整理。ホームからの再開は進行中回答を維持する。
- 終了結果は保存成功後に表示。失敗時はセッションと記録を退避可能な状態で保持する。結果集計はメモリ上に置き、永続形式を変えない。
- 教材・語彙追加・記録・設定・学習入力を画面別モジュールへ分離。共通カードと青系の主操作を使用し、狭幅は縦配置・会話選択メニューへ切り替える。
- 非同期処理にActivity種別を付け、音声再生・認識・生成を区別。接続テストは前回の確認結果であり、現在の接続保証ではない。
- 新規上限0が2分コースで無視される境界不具合を修正した。復習間隔の計算規則は変更していない。
- 本体版のみ0.6.0へ更新。依存、ChatGPT認証限定、保存PATH・Volta起動、教材承認、媒体・ごみ箱保護は維持する。
- [承認済み設計](docs/design/ux-completion.md)、[操作・実機受入](UPGRADE-0.6.0.md)、[検証](validation/ux-0.6.0.md)を入口とする。現時点の未検証事項を下の旧版記録で補完しない。

## 2026-09-20：0.5.1 PATH補完

`src/codex/search_path.rs`で起動時・システム・実行ユーザーのPATHを補完し、Codex探索とVolta本体探索・子PATHへ共通利用する。明示パス欠損時の自動代替はしない。`examples/codex_path_probe.rs`はCodexを起動せず探索だけを確認する。[操作](UPGRADE-0.5.1.md)、[検証](validation/path-search-0.5.1.md)。

## 2026-09-20：0.5.0 操作制御

- 読み上げの±5秒・pause/resume/stop・0.5〜4倍、録音pause/resume/cancel、会話ごみ箱、effort候補取得・指定、型付き永続診断を追加した。[操作手順](UPGRADE-0.5.0.md)、[設計](docs/design/interaction-controls.md)、[現在の検証](validation/controls-0.5.0.md) を入口とする。
- `src/app/audio_controls.rs`、`control_settings.rs`、`conversation_trash.rs`へUI操作を分離。音声はWinRTの非同期合成とMediaPlayer、録音は収録サンプル数で時間管理する。
- ごみ箱・復元は保存成功後に反映。削除会話への送信・教材化・添付を拒否し、復元後の古い編集ウィンドウを閉じる。保存失敗時は未確定音声を保持する。
- effortはmodel/listのモデル別候補。空欄は省略し、生成時に再検査する。実行値は返却値のみ。CLI設定・ChatGPT認証・Volta起動を維持する。
- 新しい診断ログは本文・媒体・パス・認証情報を含まない。以前からの実行台帳は本文を含むが、媒体本体は省略する。診断出力に台帳を混ぜない。
- 依存の版更新なし。既存windows 0.56.0を直接利用するためCargo.tomlの機能公開とCargo.lockの本体依存だけを追加した。Android・完全削除は対象外。

以下は旧版の履歴であり、0.5.0の合格証拠ではない。

## 2026-09-17：0.4.0 Harness への更新

以下は0.3.8移行時点の履歴である。現在の入口は [UPGRADE-0.4.0.md](UPGRADE-0.4.0.md)、[設計判断](docs/decisions/0001-windows-harness.md)、[今回の検証](validation/harness-0.4.0.md)、[残作業](tasks/todo.md) とする。

- Windows用に録音・筆跡・合成画像の保存、根拠チャットを並置した左右比較、理由と引用、実行記録の回収、媒体付きバックアップを実装した。Androidは今回対象外。
- 2026-09-17の最終ソースで全119件テストとreleaseビルドが成功した。未保存の録音・固定背景・固定前原文の終了保護も含む。残るのは実Codex媒体入力とマイク・ペン等の実機評価であり、旧版の成功を代用しない。
- `thread/start` は現在 `ephemeral=false`。各生成で独立スレッドを作る点は維持し、WordWeaveが保存・選択した文脈を再送する。永続化は結果再取得用であり、暗黙の会話記憶への移行ではない。
- 原本は `assets`、実行記録は `codex-work/wordweave-runs`。後者には指示・応答本文が入る。本文非保存という旧記述は診断トレースに限定される。
- `commit` は2ファイル更新の意図を先に保存し、再起動時に回復する。外部編集との競合は上書きせず停止する。`backup` は未保存のProgressも復元前に退避する。
- CLI 0.153.4によるgpt-6-astraのテキスト生成は利用者から成功報告済み。ただし今回の媒体入力・結果再取得の実接続成功を意味しない。実行パス設定を変更していない。
- 依存追加・更新なし。Cargo.lockの変更はWordWeave本体0.4.0だけ。実ユーザーの教材・学習フォルダーは今回の自動テストで使用しない。
- 通常sandboxはACL初期化で失敗するため承認付き実行を使用。ACLを変更していない。承認機構の利用上限による中断と、ソースの不具合を混同しない。

---

基準日：2026-09-09／対象：0.3.8。資料は保存済みソースと開発会話の報告を照合して作成した。利用者PCの現在のソース・学習データ・認証状態は直接確認していない。

## 1. 現在地点

Windows向けRustアプリである。0.3.6までのCodex起動修正について利用者から「動作したようです」と報告された。その後、0.3.7でモデル・effort表示と英語チャット、0.3.8でチャット内容の教材化を実装した。

**0.3.7／0.3.8はソース実装済みであり、コンパイル・テスト実行・Windows画面操作・実Codex接続の成功は未確認である。** まずこの差を埋める。新しい機能を追加する前に、現行ソースをWindowsで検証することが次の作業である。

今回の引き継ぎ作業ではアプリを変更していない。元ソースのバイト一致、資料中の主要仕様とソースの照合、ZIP整合性を確認した。以前の`validation/`内の合格ログは0.3.0時点の記録であり、0.3.8の証拠ではない。

## 2. 利用目的と確定要件

毎日短時間で、基本語から英語の語感・使い分けを身につける。社外メールに適した表現と、文体を変える表現を学ぶ。難しい語への一律の置き換えを目的にしない。言い換えには使える条件を付け、適切な代替がない場合は無理に作らない。

| 領域 | 決定済みの要件／現行仕様 |
| --- | --- |
| 基本学習 | 5分コース、キーボード・手書き、読み上げ、録音・再生、復習記録 |
| 教材 | 基本語、複数語の熟語、構文。基本語に複数の言い換えと複数の完成例文を持たせる |
| 語彙追加 | NGSL候補と利用者の提供語から生成・検査・登録。中断後は未処理から再開 |
| 例文 | 初期値は1語6例文。場面・意味・文法の違い、日本語訳と語感の説明を含める |
| 追加生成 | 英文追加ボタン、日本文から英文・説明の生成保存 |
| 英作文 | 日本語出題→英作文→添削→書き直し。英文の妥当性と指定表現を使えたかを分ける |
| AI接続 | ChatGPT認証の`codex app-server`。APIキー認証は拒否し、従量課金APIへ切り替えない |
| 利用枠 | ChatGPT契約の利用枠は消費する。アプリ側にも生成回数制限がある |
| 実行情報 | Codexが返したモデル・effortを表示。設定から未取得値を推測しない |
| 英語チャット | 会話を保存し、選択した文脈を再送。再起動後も続ける |
| 教材化 | 選択したチャット往復から案を作り、確認・編集後に新規登録／追加／訂正 |

内蔵教材は基本語120件＋熟語8件＋構文8件の136件である。NGSLの2,859語は別の「生成候補」であり、全語の解説・例文が作成済みという意味ではない。NGSL本体2,809語と補足52語を結合し、重複2語を除いている。`data/NGSL-NOTICE.md`の出典・CC BY-SA 4.0表記を保持する。プログラムのMITライセンスとは別である。

## 3. 環境とビルド

利用者の環境はWindows 11、Visual Studio Community 2026のC++ビルド環境、Rust/CargoのMSVC系である。最後に報告されたCLIは`codex-cli 0.144.4`。現在の版は移行先で再確認する。

`Cargo.toml`はRust 2021、package 0.3.8。GUIはeframe/egui 0.31.1、ファイル選択rfd、通信reqwest、音声tts/cpal/houndなどを使う。Windows依存はCargoのターゲット条件で分離されている。最新の追加機能で依存ライブラリを増やしていない。`Cargo.lock`を保持する。

```powershell
cargo test --all-targets --locked
cargo build --release --locked --bin wordweave5
.\target\release\wordweave5.exe
```

`build.cmd`は全テスト→releaseビルドを実行し、`build.log`へ記録する。特定の通信失敗を調べる時だけ次を使う。

```powershell
cargo test --test codex_process --locked -- --test-threads=1
```

この個別・逐次実行の成功だけで全テスト成功とはしない。補助プログラム`tests/support/mock_codex.rs`に対する`running 0 tests`は正常である。実際の通信検証は`tests/codex_process.rs`にある。現行はWindowsで14件、非Windowsで12件の通信テストを定義している。モックとの試験であり、実ChatGPTとの接続試験ではない。

## 4. ソースの案内

| ファイル | 責務／最初に見る箇所 |
| --- | --- |
| src/app.rs | egui画面、非同期生成、結果登録、教材・チャット画面、保存の呼び出し |
| src/ai.rs | 生成・添削・チャット・教材化プロンプトとスキーマ、接続設定、NGSL取得 |
| src/codex.rs | EXE探索、Volta起動、JSON-RPC、認証、イベント、終了・中断 |
| src/execution.rs | 返却モデル・effort、今回／前回の実行状態 |
| src/diagnostics.rs | 直近の診断要約、stderr分類、機密本文を残さない処理 |
| src/chat.rs | 会話・下書き・参照指定、送信コンテキスト選択 |
| src/material.rs | 教材化の新規／追加／訂正、差分検証、競合防止、元会話参照 |
| src/model.rs | Entry、TSV、検証、重複正規化、教材fingerprint |
| src/store.rs | Progress、保存、バックアップ、排他、旧データ互換 |
| src/learning.rs / scheduler.rs | 出題・学習データ・候補語解析／復習スケジュール |
| src/ink.rs / media.rs | 手書き／読み上げ・録音再生 |
| tests/codex_process.rs | 子プロセス通信・認証拒否・Volta・実行設定・履歴再送の試験 |
| tests/support/mock_codex.rs | 外部AIを呼ばない模擬サーバー |

## 5. Codex接続の設計

リクエストごとに子プロセスを開始し、標準入出力のJSON-RPCで通信する。概略はinitialize→initialized→account/read→thread/start→turn/start→完了結果である。ChatGPT認証のみ許可し、APIキー認証・未認証を区別する。子プロセス環境からOPENAI_API_KEYとCODEX_API_KEYを除く。

thread/startにはephemeral=true、modelProvider=openai、approvalPolicy=never、sandbox=read-only、web_search無効と、教材データを命令として扱わない指示を渡す。モデルはアプリの設定が空でなければ要求する。effortを明示指定する実装はない。thread/start応答のmodelとreasoningEffortを保存・表示する。欠落やnullは未取得とする。これは返却設定であり、サーバー内部の実際の計算量を測定したものではない。

接続・認証確認だけではモデル・effortを新たに取得しない。ステータスの「前回の実行設定」を現在のログイン状態の証拠にしない。

JSON行の断片化、開始応答より早い完了通知、失敗ターンの部分出力を扱う。失敗した部分英文を成功した教材・チャットとして登録しない。中断時は直接の子プロセスを終了・回収するが、Volta/CMDの子孫プロセス全体の終了は保証していない。

### Windows／Voltaで解決した問題

利用者のshimは`F:\Tools\Volta\bin\codex.cmd`で、内容は次の2行であった。

```bat
@echo off
volta run %~n0 %*
```

以前の一般CMDバッチ経路は、initialize前に終了コード1で終了した。CALLへの変更だけでは解消せず、未分類stderrしか残らなかったため、細かい根本原因は確定していない。

現在は既知のVolta shimを識別し、同じディレクトリ→PATH上の絶対ディレクトリの順でvolta.exeを探索し、`volta.exe run codex app-server`を直接起動する。0.3.5の同一ディレクトリ探索だけでは利用者環境で不足し、0.3.6でPATH探索を追加した。一般のcmd/batは従来のCALL経路が残る。この一般経路まで検証済みとは扱わない。

過去の別問題として、config.tomlの[agents]にCLIが認識しない整数設定があり、AgentRoleTomlの構文エラーになった。利用者の設定修正でエラーはなくなった。また、OAuth開始URLの表示だけではログイン完了ではなかった。これらの過去の問題を今回の失敗原因と決めつけない。

## 6. チャットの記憶と教材化

コンテキストの保管者はWordWeaveである。質問ごとに新しいCodexスレッドを開始し、WordWeaveが履歴を選んで再送する。thread/resumeは使わない。ブラウザのChatGPTや別会話の履歴は自動参照しない。

会話には質問・成功した回答・時刻・返却実行設定、下書き、メモを保存する。「次回も参照」を付けた往復とメモ・今回の質問を優先し、残る容量で直近の連続した履歴を追加し、時系列で送る。送信対象外でも保存履歴は消さない。送信内容のプレビューがある。

上限は質問4,000文字、メモ2,000文字、回答16,000文字、1会話200往復、50会話、全会話20MB。通常チャットの送信JSONは64,000 UTF-8 bytesまでであり、トークン数制限ではない。参照指定だけで上限を超えた場合は送信を止める。自動要約・意味検索による関連履歴の自動選択は未実装である。

教材化の「教材に反映するやり取り」は次回参照指定と別のフラグである。選択した往復と対象教材だけから案を生成する。未選択の会話やメモを自動的に含めない。教材化JSONの上限は128,000 bytes。案は1件まで保存し、登録／破棄を待つ。

| モード | 登録動作と保護 |
| --- | --- |
| 新規 | 新ID。既存と同じ基本語なら別用法としての明示確認。完成例文2件以上が必要 |
| 追加 | 既存の基本項目・例文・言い換えを保持し、新しい補足だけを加える。既存の説明変更案は注意表示して採用しない |
| 訂正 | 既存項目を編集・削除できる。変更前後と復習への影響を表示する |

生成後の対象教材が変わっていたら古い案の登録を拒否する。競合検査では補足も含むTSV全体を照合する。登録時に形式検証し、同一案の二重登録・変更なしも拒否する。重複判定は正規化文字列であり、意味の同一性や英文の正しさを保証しない。

教材に元のconversation_idと選択往復の添字、反映方式・時刻を関連付ける。元の会話を後から訂正しても教材は自動変更しない。参照先会話そのものの複製は保存していないため、会話データも一緒に移行する。

## 7. 保存と互換性

通常保存先は`%LOCALAPPDATA%\WordWeave5`である。

| 対象 | 内容 |
| --- | --- |
| progress.json | 設定、復習記録、生成回数、英作文履歴、日本文、chats、material_draft、material_sources |
| custom.tsv | マージ済みの教材。名前から「追加分だけ」と判断しない |
| generation-queue.json | 語彙生成の再開情報 |
| words.csv | 語彙候補キャッシュ（存在する場合） |
| backups等 | 学習記録／教材のバックアップ |
| app.lock | アプリ多重起動防止 |
| codex-work | AI子プロセスの作業フォルダー |

教材は旧15列と、言い換え・完成例文配列を加えた17列TSVに対応する。追加例文・言い換えだけなら既存復習状態を維持する。基本15項目のfingerprintが変わる場合は当該教材の復習状態を再学習に戻す。分類・タグ等も基本項目に含まれる。既存履歴全体を削除する意味ではない。

Progressの追加フィールドはserde defaultで旧データを読む。新しい項目を知らない旧版で保存し直すと、チャットや教材案・参照を失う可能性がある。更新後のデータを旧版に書き戻さない。

custom.tsvとprogress.jsonは単一トランザクションではない。教材保存後にProgress保存が失敗する経路があり、エクスポートで復旧する設計である。原子的な「両方同時更新」ができるとは扱わない。教材TSVだけの受け渡しでは会話・参照・復習記録を移行できない。

## 8. 検証の経緯

| 版 | 主な変更 | 証拠と限界 |
| --- | --- | --- |
| 初期版 | Windows英語学習アプリ、f32警告修正 | 利用者がビルド・起動成功を報告 |
| 0.2 | 語彙取り込み、熟語・構文、日本語出題と添削 | 旧接続方式。現行版の基準にしない |
| 0.3.0 | Codex接続、NGSL、複数例文、自動登録 | 旧29テストとWindows型検査の記録。Windows通信試験は当時Unix限定 |
| 0.3.1 | Rust製モック、Windowsでも通信試験を登録 | 0件問題を改善。補助binの0件表示は正常 |
| 0.3.2〜0.3.4 | 診断、終了状態待ち、バッチ起動調整 | 利用者試験で最初8/10、後9/10。一般バッチ失敗が残った |
| 0.3.5 | Volta直接起動・同一フォルダー探索 | 利用者の10件試験は成功したが、実Volta起動はなお失敗 |
| 0.3.6 | 既知shim検出＋PATH探索 | 利用者が動作した旨を報告。全機能の詳細試験結果ではない |
| 0.3.7 | 実行設定表示、履歴付き英語チャット | 静的確認のみ。通信テスト3件追加、Windows計14件 |
| 0.3.8 | チャット教材化 | 静的確認のみ。教材化単体テスト6件追加、未実行 |

## 9. 既知の制約と今後の候補

優先するのは未検証の現行版の成立確認である。その後の候補として、2ファイル保存の整合性改善、子孫プロセス終了、一般バッチ起動、未知のstderrを安全に分析できる診断改善、長い会話の整理がある。これらは今回実装済みではなく、追加開発を決定したわけでもない。

診断は直近1回・最大128件をメモリに保持し、stderr本文は保存せず固定分類へ変換する。未知のエラーやWindows文字コードは未分類になり得る。診断のコピー／保存とChatGPTを開く導線はあるが、自動解析や自動送信ではない。秘密情報を含む認証URLやauth.jsonを診断資料として要求しない。

参考仕様への入口は既存のRESEARCH.mdとREADME.mdにある。APIやCLIの更新を調べる場合は、移行先にインストールされた版の仕様と公式資料を再照合する。今回の引き継ぎでは新しいCLIの互換性を確認していない。
