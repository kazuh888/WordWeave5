# WordWeave5 0.5.0：音声操作・会話ごみ箱・effort・診断ログ

2026-09-20。Windows版である。検証結果と未確認事項は [今回の検証記録](validation/controls-0.5.0.md) を参照する。0.4.0以前の成功を今回の合格証拠にはしない。

## 更新前

WordWeave5を閉じ、`%LOCALAPPDATA%\WordWeave5` 全体を別フォルダーへコピーする。教材・学習記録・録音・筆跡は新旧プログラムで同じ保存先を使う。更新後の記録を旧版で保存し直すと、ごみ箱や新しい設定が失われるため行わない。

CLI 0.153.4の実行ファイル設定、ChatGPT認証、Volta起動・PATH探索を維持する。APIキーへのフォールバックはない。既存の依存バージョンは更新せず、すでに依存していたWindows API 0.56.0を直接利用する。

## 読み上げ

回答の「回答を読み上げ」または設定の「音声を確認する」を押すと、画面下部に操作欄が現れる。タブを切り替えても操作できる。

- 「5秒戻る」「5秒進む」：音声の先頭・末尾を超えない範囲で移動する。
- 「一時停止」：位置を維持する。「再生」で再開する。
- 「停止（先頭へ）」：停止し先頭へ戻す。合成準備中なら合成をキャンセルする。
- 再生位置スライダー：秒数を確認しながら移動できる。準備中は無効である。
- 0.5〜4倍の速度：再生中にも変更でき、次回の読み上げにも保存される。旧版の「少しゆっくり」は初回0.8倍として引き継ぐ。

原録音の再生は従来の方式であり、この操作欄は読み上げ用である。Windowsの英語音声と出力デバイスが必要であり、実際の音質はデバイス・音声ごとに確認する。

## 録音

「録音を一時停止」「録音を再開」で収録を中断・再開できる。一時停止中の時間や無音を挿入せず、収録した音声だけを最大30秒まで保存する。

「録音を終了して保存」と「録音をキャンセル…」は別である。キャンセルは確認後に今回の未確定録音だけを破棄する。過去の録音や教材は削除しない。学習画面の録音も同じ操作が可能であるが、従来どおり次の問題までメモリ保持となる。

録音中は読み上げを開始できない。読み上げを停止できなかった場合も録音を開始しない。デバイス障害時は取得できた有効な音声を保持し、部分録音である旨を表示する。保存に失敗した音声は入力画面から再保存またはWAV退避できる。

## 添付と会話の削除

「添付を外す」は、今回の下書きへの添付を解除する操作である。媒体原本、過去の履歴、文字起こしから入力欄へ反映済みの文章は削除しない。

会話の「会話を削除…」から確認してごみ箱へ移動できる。「チャットのごみ箱」で読み取り専用の閲覧と復元ができる。教材・復習記録・教材の根拠・録音・筆跡・Codex側の履歴は削除しない。完全削除や自動消去はない。

通常50会話、ごみ箱500会話、両者合計20MBまでである。上限で復元・移動できない場合は理由を表示し、原本を勝手に消さない。処理中・未保存媒体編集中・対象会話の未承認教材案がある場合は移動できない。

## effortの指定

1. 設定→AI接続でCLIのパスを確認し、「モデル・effort候補を取得」を押す。
2. 取得したモデルから選択し、「effort（推論強度）」でサーバーが返した候補を選ぶ。
3. 「Codexの既定値（指定しない）」ならeffortを送らない。モデルが空欄で候補不明の時は、推測した選択肢を出さない。
4. 少量のチャットで生成し、ステータスバーの実行モデル・effortを確認する。これはサーバー返却値であり、指定欄や候補一覧の既定値とは別である。

モデルやCLIパス変更時に、保存済みeffortを勝手に変更しない。生成開始時に最新候補と実際に返ったモデルを再検査し、未対応なら生成を止める。WordWeave内だけに保存し、Codex共通設定は変更しない。

## 問題が起きた時

設定→「診断ログ（再起動後も保持）」→「診断ログを読み込む／更新」で確認し、コピーまたはファイルに保存する。共有は手動であり、自動送信しない。

日時・アプリ版・処理ID・入口・段階・結果・エラー分類・所要時間を記録する。入力文、回答、原音、筆跡、ユーザーパス、認証情報、サーバーエラーの原文は含めない。このため詳細原因をログだけで特定できない場合もある。

保存先はアプリ保存フォルダー内の`diagnostics`。最大5ファイル・各1MiB・30日で、管理対象の古い診断ログだけを削除する。教材・実行記録・媒体は削除しない。書き込み中断による最終行の欠落は出力から除き、次ファイルへ記録を続ける。途中の不正行や未知項目は出力を拒否する。診断の保存失敗は、本来の教材保存・生成の成否と分けて表示する。

従来の直近Codex診断はメモリ上の要約である。本文を含む実行記録、原音・筆跡の原本は別保存であり、診断ログの出力に混ぜない。

## 実機確認

- 読み上げで一時停止・再開、5秒前後、先頭・末尾、0.5/1/2/4倍、再生中の速度変更を確認する。
- 録音を数秒→一時停止→再開→保存し、停止中の無音が入らないことを確認する。キャンセル後も過去の録音が残ることを確認する。
- 会話をごみ箱へ移動・閲覧・復元し、教材と学習記録が変わらないことを確認する。
- CLI 0.153.4でモデル候補取得と少量の実生成を行い、指定値と返却値を分けて確認する。
- アプリ再起動後に診断ログが読め、入力した文や認証情報が含まれないことを確認する。

自動テストは隔離データと模擬Codexを使う。これらの実マイク・実音声・実Codex確認の代わりにはならない。
