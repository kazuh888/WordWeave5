# 英語チャットの評価手順

対象は0.3.8を基にした今回の未コミット変更である。旧版の検証結果を今回の合格証拠にはしない。

## 操作

1. 「英語チャット」を開き、左の「＋ 新しい会話」を押す。
2. 右下へ質問を書き、「送信」またはCtrl+Enterで送る。通常のEnterは改行、日本語変換の確定に使ったEnterは改行しない。日本語変換の確定中はショートカットで送信しない。
3. 左右の境界をドラッグすると会話一覧の幅が変わる。入力欄上辺のドラッグ領域では高さを変更できるが、説明文と追加の線は表示しない。
4. あなたの発言は右寄せの緑系、Codexの回答は左寄せの灰色系で表示する。
5. 回答時にタイトルを自動生成する。左の☆で会話をピン留めする。ピン留めした会話を先頭にし、各グループ内は最後の回答時刻順である。

## チャットから教材を操作する

- 整理：「この会話で出てきた apologize for の使い方を整理して」
- 新規登録：「apologize for を新規登録して」
- 追加：「apologize for に、この会話の例文を追加して」
- 削除：「apologize for の教材を削除して」

操作は一度に一つの単語・表現が対象である。曖昧な指定や複数語の指定は確認の回答になる。AIの解釈は提案であり、回答だけで教材が書き換わることはない。

新規登録・追加は、確認画面から「根拠と教材案を確認する」へ進む。根拠にする往復を選び、「教材案を作成」を押す。生成された差分を確認・編集し、最後に「内容を確認して教材に登録」を押す。未登録の案があるときは、先にその案を登録または破棄する。

追加は既存の基本説明・問題・正解・復習状態を維持し、例文と言い換えを追加する。既存内容の訂正とは別モードである。

削除は確認した教材を一覧・出題から外す操作である。教材本体と復習記録は保持し、左の「削除済みの教材」から復元できる。同名の教材が複数ある場合は意味とIDを見て対象を選ぶ。

## コンテキスト

会話履歴はWordWeaveが保存し、毎回選択して送り直す。他の会話の履歴は混ぜない。

- 引き継ぎメモ、今回の質問、「文脈に必ず含める」に指定した往復を優先する。
- 直近の往復を確保し、質問と語句が一致する古い往復も選ぶ。
- 往復の途中では切らず、時系列に戻して送る。対象外の履歴もローカルには残る。
- 教材一覧を含む送信JSONを64,000バイト以内にする。これはモデルのトークン上限そのものではない。
- 「文脈・送信内容」から送る内容、往復番号、省略件数を確認できる。送信予定の確認には入力欄に質問が必要である。

参照したCodex CLI実装は `F:/Kazuhiro/GitHub/codex/codex-rs/core/src/context_manager/history.rs` と `core/src/compact.rs` である。履歴の保存とモデルに送る範囲の分離、最新発言の確保、時系列の保持を参考にした。CLIの自動要約・トークン計測を移植したわけではない。

## 制約

ChatGPT認証のcodex app-serverのみを使用する。APIキー課金への切替はない。モデル・effortはサーバーから返った値のみを表示し、未取得は未取得のままとする。

会話は50件、1会話200往復、入力4,000文字までである。スプリットの寸法は起動中に保持するが、再起動時は初期寸法に戻る。実Codexによる新しい操作判定・タイトル品質、最大量の履歴でのUI応答性は別途評価が必要である。
