# 0.3.8：チャットを教材へ反映

## 更新・ビルド

旧版の設定から学習記録をエクスポートして保管し、旧版を閉じる。新しいフォルダーに展開し、Cargo.tomlがあるwordweave5フォルダーで実行する。

```powershell
cargo test --all-targets --locked
cargo build --release --locked --bin wordweave5
.\target\release\wordweave5.exe
```

依存ライブラリの追加はない。通信テストはWindowsで14件を維持し、教材化の単体テスト6件を追加した。mock_codex.rsの単体テスト0件は正常である。

今回の環境にはRustコンパイラとWindows実行環境がない。コンパイル・テスト実行・画面操作・実Codexによる教材案生成は未確認であり、ソース・TOML・ZIPの静的確認のみ実施している。

## 操作

1. 「英語チャット」で会話を開く。
2. 教材の根拠にしたいやり取りに「教材に反映するやり取り」を付ける。誤りが訂正された会話では、訂正を含むやり取りも選ぶ。
3. 画面下の「チャットを教材に反映」で対象の基本語・熟語を入力する。
4. 新規登録／追加／訂正を選ぶ。追加・訂正では、同じ基本語の候補から意味・場面を見て反映先を選ぶ。
5. 「教材案を作成」を押す。選んだ質問・回答と既存教材をCodexに送り、教材形式に整理する。生成回数を1回使用する。
6. 「基本の説明・問題」「言い換え」「完成例文」で確認・編集し、「登録される差分」で変更前後を確認する。
7. 「内容を確認して教材に登録」を押す。このボタンを押すまでは学習教材へ反映しない。

## 登録方法の違い

| 方法 | 動作 |
| --- | --- |
| 新規登録 | 新しい教材IDを割り当て、意味・用法・問題・言い換え・複数の完成例文を登録する。同じ基本語がある場合、別用法として新規登録する確認が必要 |
| 追加 | 基本の説明・問題・正解と既存の例文・言い換えを維持し、新しい例文・言い換えだけを加える。語感・文法の補足は例文の説明や言い換えの使用条件に記載する |
| 訂正 | 既存の説明・問題・正解・例文・言い換えを修正できる。削除も差分に表示するため、不要な変更がないか確認する |

同じ基本語でも別の意味・用法があるため、複数の教材を自動的にまとめない。対象語は綴りを入力する。複数語の熟語も指定できる。

追加モードで、同じ英文の訳や説明・同じ言い換えの使用条件が異なる案が返った場合、既存内容を維持して注意点を表示する。変更するには訂正モードで案を作り直す。重複判定は大文字小文字・空白・末尾句読点等を正規化した文字列比較であり、意味が似た別の英文を判定するものではない。

新規教材には完成例文2件以上が必要である。プロンプトでは3〜6件を求めるが、生成内容の意味・文法の正確さまで機械的に保証するものではない。誤った学習者の英文や、後で訂正された過去の回答が正しい教材として残っていないか確認する。

## 復習記録と競合

追加例文や言い換えだけの変更では既存の復習成績を維持する。基本15項目（意味・用法・問題・正解・分類等）が変わる場合、その教材の復習状態は既存の仕様に従って再学習へ戻す。画面に予告を表示する。

案の生成後に別操作で反映先の教材が変わった場合、古い案で上書きせず、案の作り直しを求める。変更がない案や、同じ新規案の二重登録も拒否する。

## 下書きと元の会話

確認中の教材案は1件まで保存し、登録・破棄まで保持する。編集途中の空欄は保存できるが、登録時には全項目を検査する。新しい案を作る前に現在の案を登録または破棄する。

教材画面の「教材の元になった会話」から元のチャットを開ける。反映時に選択したやり取りの番号も表示する。元の会話が後から更新されても、教材を自動で書き換えることはない。

教材はcustom.tsv、確認中の案と会話への参照はprogress.jsonに保存する。会話と参照を移行するときは「学習記録をエクスポート」も使用する。教材TSVだけの移行では会話への参照は付かない。

0.3.7以前は追加した保存項目を保持しない可能性があるため、更新後の学習記録を旧版で上書きしない。保存失敗時は画面の指示に従い、現在の学習記録と教材をエクスポートする。
