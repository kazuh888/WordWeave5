# WordWeave 5 0.3.0 更新と使い方

0.1/0.2からの更新で、AI接続を `codex app-server` に変更した。

## 更新

1. 旧版を終了する前に「設定」から学習記録JSONと教材TSVを書き出す。
2. 旧版を終了し、必要なら `%LOCALAPPDATA%\WordWeave5` フォルダー全体もコピーする。
3. このZIPを新しいフォルダーへ展開する。旧版のソースへの上書きは不要である。
4. `build.cmd` を実行する。既に用意したVisual StudioのC++開発環境とRustを使用できる。
5. `run.cmd` で起動する。保存先は従来と同じであり、既存の記録・教材を自動的に読み込む。

0.3は旧15列TSVと旧JSON設定を読み込める。新しい教材は17列TSVで保存するため、0.3で追加した教材を旧アプリに戻して読み込ませることはできない。戻す場合は手順1の旧形式バックアップを使う。

## Codex接続の準備

Codex CLIがインストール済みなら、新しいPowerShellで次を実行する。

```powershell
codex --version
codex login
```

ChatGPTアカウントでログインする。APIキーでログインしている場合は、必要に応じて `codex logout` 後に `codex login` を実行する。これは他のCodex利用にも影響するため、アプリ自身はログアウト処理を行わない。

Codex CLI未導入の場合は[公式CLI手順](https://learn.chatgpt.com/docs/codex/cli)を参照する。Node.js/npmで導入する場合のコマンドは次のとおりである。

```powershell
npm install -g @openai/codex
```

ChatGPTデスクトップアプリを入れただけで `codex` コマンドが使用できるとは限らない。

アプリの「設定」で次を行う。

1. 実行ファイルの初期値は `codex`。PATHとnpmの標準インストール先からネイティブ実行ファイルを探す。
2. 見つからない場合は「Codex実行ファイルを選択」で実体のEXEを指定する。`codex.cmd`、`codex.bat`、または`codex.exe`を指定できる。`ChatGPT.exe`は指定しない。パスに引用符や `app-server` 引数は付けない。
3. モデル名は空欄ならCodexの既定値を使用する。利用できるモデルを明示したい場合だけ入力する。
4. 「接続・ChatGPT認証を確認」を押す。これは教材生成回数に数えない。
5. 「設定を保存」を押す。

接続は子プロセスの `codex app-server` と標準入出力で通信する。毎回 `account/read` でChatGPT認証を確認し、APIキー認証や未ログインなら生成前に停止する。有料APIへのフォールバックはない。WordWeaveの旧APIキー、旧モデル設定、Windows資格情報は利用しない。

生成はChatGPT契約の利用枠を消費する。「無料・無制限」という意味ではない。利用枠到達時は停止し、自動で課金方式を変更したりクレジットを購入したりしない。1日の生成試行上限は初期値10回で、設定から0〜1,000回に変更できる。失敗した試行も数える。

## NGSLから教材を自動登録する

1. 「語彙を追加」を開く。
2. 「1回に追加する語数」を指定する。初期値は5語。全語を順次処理する場合は3,000語にする。
3. 「1語あたりの例文数」を指定する。初期値6、範囲3〜12である。
4. 「NGSLから自動生成・登録」を押す。
5. 語ごとに言い換え・条件・空欄問題・使い分け問題・複数例文を生成し、形式を検査して自動保存する。

同梱データは公式NGSL 1.2の頻度順2,809語と補足52語。2語の重複を除き2,859語である。これらは基本語候補であり、全語分のAI例文をあらかじめ生成済みではない。上記ボタンで実行時に教材化する。

「公式NGSLを再取得して自動登録」は公式サイトからCSVを取得した後、同じ自動処理を行う。取得失敗時はエラーを表示し、同梱リストやダウンロード済みCSVから処理できる。

処理は同時に1語だけ進める。生成済みの自動教材は重複登録しない。旧内蔵教材は別IDで保持する。「生成・通信を中断」で止めると、登録済みの教材と未処理の語を保持する。再起動後は「未処理の語から再開」を押す。認証エラー・利用上限・不正な応答の場合も中断し、問題の語を未処理として残す。生成待ちは1回最大5分である。

例文は複数の場面、語義、文法、言い換えの可否を説明するよう生成する。言い換えが自然な語では複数候補を要求する。冠詞など、適切な言い換えがない語には無理に候補を割り当てない。

## 自分が提供する基本語

「語彙を追加」で、1行1語（熟語は1行に1項目）を入力して「入力した語から自動生成・登録」を押す。CSV・TSV・TXTの読み込みボタンからも自動登録できる。UTF-8を使用する。

例：

```text
investigate
provide
look into
```

指定した1回の件数を超える語も候補として保存する。同じ入力で再度実行すると、生成済みを除いた続きから処理する。

## 英文を追加する

「教材」で語を選び、「英文追加：新しい場面の例文を生成・登録」を押す。初期値では6例文を追加する。各例文に日本語訳と使い方の解説を保存する。1教材200例文まで登録できる。

「語感を掴む例文」を開くと一覧と読み上げボタンが表示される。言い換えだけを増やしたい場合は「言い換えを手動登録」を使う。言い換え候補は1教材30件までである。

## 日本文から英文を生成・登録する

1. 「教材」で関連する語を選ぶ。
2. 「日本文を登録して英文を生成」を開く。
3. 原文を入力し、「日本文を登録し、英文を生成・登録」を押す。
4. 原文を先に保存し、英文と解説を生成して例文一覧に追加する。

翻訳に失敗しても日本文は下書きとして残る。対象語を入れると意味が変わる場合は無理に使用せず、原文の意味を優先する。

## 保持される機能と確認範囲

5分学習・復習・熟語/構文教材・日本語出題・英作文の添削・書き直し履歴・手書き・読み上げ・録音再生を継続する。出題・添削・画像からの手書き認識もCodex接続を利用する。

録音の自動文字起こしには、音声入力に対応したCodex CLIとモデルが必要である。実行前にモデルの対応状況を確認し、非対応なら説明を表示して停止する。録音再生と手入力はモデルに関係なく利用できる。音声入力の連携は実サービスでは未確認である。

追加例文だけの変更では既存の復習成績をリセットしない。空欄問題・解答・用法など既存15列を変更した場合は従来どおり再学習に戻す。自動生成の形式検査は英語の正確さを保証するものではない。教材画面で編集でき、AIの添削は自動で学習成績を変更しない。

詳細な検証結果は `VALIDATION.md` に記載する。
