# WordWeave5 0.4.0：Windows Harness

2026-09-17。音声・筆跡を残すチャット、根拠付き教材比較、生成結果の回収、媒体付きバックアップを追加した。Androidは対象外である。検証結果は [validation/harness-0.4.0.md](validation/harness-0.4.0.md) を参照する。実Codexやマイクの合格をモック試験で代用しない。

## 更新前

WordWeaveを終了し、`%LOCALAPPDATA%\WordWeave5` 全体を別の場所へコピーする。新旧プログラムは同じ保存先を使う。更新後の記録を旧版で保存すると、新しい添付・根拠情報が失われる可能性がある。

CLIの実行ファイル設定とChatGPT認証はそのまま使う。利用者が指定したCLI 0.153.4をこの更新で別版へ置き換えることはない。依存ライブラリの更新、APIキーへの切替は行っていない。

## チャットの音声・手書き

1. 英語チャットで会話を選び、音声入力・手書き注釈の画面を開く。
2. 音声は最大30秒録音し、停止して原録音を保存する。「Codexで文字起こし」を選んだ時だけ音声を送る。結果は入力欄で確認・訂正し、Ctrl+Enterまたは送信ボタンで送る。認識中に編集した下書きは上書きしない。
3. 手書きは「空白面に手書きする」、英文への注釈は本文を貼り付けて「英文を固定して注釈を書く」を選ぶ。丸・矢印・取消線を描き、画像と筆跡を添付・保存する。
4. 送信画像を確認し、「丸囲みの語の使い方を説明して」など質問を添えて送る。取消線だけで教材を削除することはない。
5. 回答の読み上げはWindowsの音声を使う。停止操作を備える。履歴・教材化の固定根拠から原録音や注釈画像を参照できる。

原録音WAV、筆跡JSON、背景PNG、合成PNGを別ファイルとして保持する。通常のチャット送信では音声本体を毎回送らず、確認した文字と今回の添付画像を送る。過去の添付画像も毎回すべて送り直すわけではない。

モデルが音声・画像入力に対応するとサーバーから確認できない場合は、送信前に理由を表示して停止する。テキスト生成が成功しても音声・画像入力の対応証明にはならない。未対応時も文字入力を使用できる。

保存に失敗した録音・注釈はメモリに残す。回復画面からWAV、または筆跡・背景・合成画像一式を別の保存先へ退避できる。「固定」前の原文はTXTへ退避でき、空白面への切替で消さない。全ファイルの保存成功を確認するまでウィンドウを閉じない。明示的な破棄も可能である。アプリの正常終了時には未保存媒体・原文を警告するが、強制終了・電源断でメモリ上の内容まで保護できるわけではない。

## 教材操作と差分

チャットで単語の整理、新規登録、追加、削除を依頼できる。返答から教材操作が提案されても、自動登録・自動削除はしない。

教材化する往復を選び、新規・追加・訂正の案を作る。比較画面の左に変更前／登録後、右に生成時点で固定した根拠チャットを表示する。変更箇所にマウスを重ねると取得済みの理由を表示し、選択すると右の一致する引用を強調する。理由がない箇所は「未取得」とし、推測で作らない。

項目ごとに比較し、例文は正規化した英文、言い換えは正規化した表現で対応づける。英文そのものが変わる場合は削除＋追加となる場合がある。文中はUnicode文字単位の差分である。これは表示の比較であり、英語の意味や文法を判定するものではない。

- 追加：既存の基本説明・既存例文・復習状態を保持する。基本説明の修正が必要なら訂正として確認する。
- 訂正：既存内容の変更・削除を含められる。復習状態への影響を確認して登録する。
- 手動編集後：AIが生成時に付けた理由を、編集後の差分の理由として流用しない。固定根拠は残る。

## 結果不明と再取得

上部の「実行記録」から生成の状態と取得済み本文を確認する。新しい200件まで表示し、古いファイルを自動削除しない。

| 表示 | 判定 |
| --- | --- |
| 生成完了 | Codexの完了と最終本文を取得・保存した。形式検証・教材登録の成功とは別である |
| 失敗 | 送信前の失敗、またはCodexが当該往復の失敗を返した |
| Codexの中断を確認 | Codexが当該往復の中断を返した |
| 結果不明 | 送信後の切断、ローカル中断、未完了記録などで完了を確認できない |

「この往復の結果を再取得」は保存済みのthread IDとturn IDに一致する結果だけを読む。別の最新往復で代用せず、再生成や教材登録も行わない。本文は必要に応じてコピーし、内容を確認する。ID未取得、サーバーに履歴がない、未完了の場合は回収を保証できない。生成指示と応答はローカルにも保存するため、機微情報の扱いに注意する。

## バックアップと保存途中からの復旧

設定の「媒体を含めてバックアップ」で、新しいフォルダーへ教材・学習記録・固定根拠・媒体原本を一式退避する。添付から外した原本も含む。`learning-backup.json` が完了マーカーであり、ないフォルダーは復元しない。NTFSなどハードリンク対応の保存先が必要であり、USBのファイルシステムによっては直接出力できない。

「媒体付きバックアップを復元」は整合性を検査し、件数を表示した後に確認を求める。現在の未保存下書きとディスク上の旧記録を退避し、既存媒体は削除しない。JSONだけの旧エクスポートには媒体本体が入らない。

媒体付きバックアップにはCodex認証、Codex側のスレッド、実行記録、処理待ち語彙一覧は含まない。すべてのローカル記録の退避には、アプリ終了後の保存フォルダー全体のコピーを併用する。

教材と学習記録の2ファイル更新は、先に保存意図を書き、途中停止した場合に起動時に回復する。保存途中のファイルと外部編集が競合した場合は上書きせず停止する。`pending-material-commit.json` がある時は自己判断で削除せず、フォルダーを丸ごと保全する。

## 実機で確認する項目

- 設定のCLI実行パスが0.153.4のままで、接続確認・短いチャット生成が成功すること。
- 日本語IME確定では改行せず、通常Enterは改行、Ctrl+Enterは送信となること。
- マイク録音→原音再生→認識→訂正→送信と、読み上げ停止。
- Windowsタブレットで丸・矢印・取消線を描き、プレビューとAIに伝わる画像が一致すること。
- 根拠チャットを横に見ながら理由hover・引用強調・追加／訂正を評価すること。
- 実Codexの結果不明時の再取得。これは新規機能であり、過去のテキスト生成成功とは別の確認である。
