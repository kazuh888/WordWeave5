# 0.5.1：Windows PATH探索の補完

## 変更

「設定」→「AI接続」の実行ファイル欄が `codex` のとき、次の順に探索する。

1. WordWeaveの起動時に受け取ったPATH。
2. Windowsに保存されているシステム環境変数のPATH。
3. Windowsに保存されている実行ユーザーの環境変数のPATH。
4. 従来のnpmインストール先。

各PATH内の順序は保持し、同じフォルダーの重複を除く。空・相対フォルダーは探索しない。`codex.exe`、`codex.cmd`、`codex.bat`に対応する。Volta shimの場合は同じフォルダー、次に上記PATHから `volta.exe` を探し、従来どおり直接起動する。子プロセスだけに補完済みPATHを渡す。

環境変数・レジストリ・CLIのインストール・保存設定は変更しない。ユーザーPATHは通常ログイン中の利用者に一致するが、「別のユーザーとして実行」した場合はその実行アカウントのものとなる。

## 利用方法

1. WordWeaveを終了し、新しい `target/release/wordweave5.exe` を起動する。バージョンは0.5.1である。
2. 自動検出する場合は、設定の実行ファイル欄を `codex` にする。
3. 特定のCLIを使う場合は「Codex実行ファイルを選択」でそのファイルを選ぶ。複数版がある場合も、版の新旧ではなくPATHの順序で選ばれる。
4. 「接続・ChatGPT認証を確認」を実行し、必要ならモデル・effort候補を再取得する。

以前保存したパスが消えていても、別のCLIへ黙って切り替えない。エラー内に上記の復旧操作を表示する。PATHで実行ファイルが発見できることと、CLI 0.153.4や指定モデルが使用できることは別である。本更新はCLIの版を変更しない。

## 検証と原本保護

[今回の検証記録](validation/path-search-0.5.1.md)を参照する。教材・学習記録・音声原本の保存形式は変更しない。0.5.0の機能操作は [従来の手順](UPGRADE-0.5.0.md) を参照する。
