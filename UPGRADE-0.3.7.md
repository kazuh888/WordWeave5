# 0.3.7：実行設定の表示と英語チャット

## 更新

旧版の設定から学習記録をエクスポートして保管し、旧版を閉じる。このZIPを新しいフォルダーへ展開し、Cargo.tomlのあるwordweave5フォルダーで実行する。

```powershell
cargo test --all-targets --locked
cargo build --release --locked --bin wordweave5
.\target\release\wordweave5.exe
```

Windowsのcodex_process通信テストは14件となる。mock_codex.rsの単体テスト0件は正常である。既存のVolta探索修正は維持している。

依存ライブラリの追加はない。既存の教材・学習記録を読み込む。会話はprogress.jsonの追加項目chatsに保存する。0.3.6以前はこの追加項目を保持せずに保存する可能性があるため、チャット開始後の記録を旧版で上書きしないこと。

## モデルとeffort

画面下部に次を表示する。

- 起動直後：モデル・effortとも「未確認」。
- 生成の開始準備中：「Codexの実行設定を確認中」。
- thread/startの応答後：「今回の実行設定」と、Codexが返したモデル・effort。
- 処理終了後：「前回Codexが返した実行設定」。
- effortが返らない場合：「未取得（Codexにお任せ）」。モデル名や設定から推測しない。

実行設定の取得は生成時に行う。「接続・ChatGPT認証を確認」だけでは新しい実行設定を取得しない。設定画面でモデル名を変更しても、前回の表示は過去の記録である。各チャット回答の下にも、その回答時のモデル・effortを保存して表示する。これはCodexが返した設定の表示であり、サーバー内部の計算量を測定するものではない。

## 英語チャット

1. 上部の「英語チャット」を開き、「新しい会話」を押す。
2. 英語の意味・文法・語感・社外メールの表現などを質問して「送信」を押す。
3. 回答後、そのまま追加質問を送る。直近の質問と回答もCodexへ送られる。
4. アプリを閉じた後も、会話の一覧から選んで続けられる。

生成中は画面下部の「生成・通信を中断」で中断できる。失敗や中断時は質問の下書きを残し、成功していない回答を次回の履歴に入れない。下書きは送信前と通常の自動保存・終了時に保存する。学習コース中は、コースを終了してからチャットを送信する。

送信1回を既存の1日の生成回数に数える。AI接続はこれまでと同じChatGPT認証のcodex app-serverである。

## 誰がコンテキストを保持するか

| 担当 | 保持・処理するもの |
| --- | --- |
| WordWeave | 会話履歴・下書き・引き継ぎメモをPCに保存し、今回送信する履歴を選ぶ |
| Codex app-server | 渡された質問と履歴を使って、その回のLLM呼び出しと応答を処理する |
| LLM | 今回渡されたコンテキストを参照して回答する。アプリの会話を自動的に永久記憶する前提は置かない |

今回の方式では、質問ごとに新しいCodexの会話を開始し、WordWeaveが履歴を再送する。Codexにもthread/resumeで会話を再開する方式はあるが、この版はそれに依存しない。ブラウザのChatGPTや他の会話の履歴は自動取得・送信しない。

履歴は保存されても、全部が毎回LLMへ送られるとは限らない。「次回送信」に含める往復数と対象外の往復数を表示する。過去の特定の話題を参照したい場合は、履歴を検索して「次回も参照」を付ける。これは質問と回答の組を優先して送る指定である。

「引き継ぎメモ」には、学習目的、重要な前提、訂正済みの内容を記入できる。メモは毎回送る。「Codexへ送る内容を確認」で実際の質問・メモ・履歴を確認できる。古い会話の自動要約や、関連発言の自動検索・選択は行わない。

## 上限

- 質問4,000文字、引き継ぎメモ2,000文字、回答の保存16,000文字。
- 1会話200往復、50会話、チャット全体20MBまで。
- 1回の送信対象はJSON換算で64,000 UTF-8 bytesまで。直近のやり取りを優先し、古い会話もローカルの履歴として残す。
- 参照指定した履歴・メモ・今回の質問だけで上限を超えた場合は、指定を減らすまで送信しない。

長い履歴を渡すほど回答時間や利用枠の消費が増える場合がある。過去の誤った説明も履歴に含まれるため、重要な訂正はメモにも明記する。

## 保存と検証

保存先は `%LOCALAPPDATA%\WordWeave5\progress.json`。会話も設定画面の「学習記録をエクスポート」に含まれる。

作業環境にRustコンパイラとWindows実行環境がないため、0.3.7は未コンパイル・テスト未実行・Windows画面未確認・実Codex接続未確認である。ソース・TOML・配布ZIPの静的確認のみを実施した。

実機では、初回質問と追加質問、再起動後の継続、別会話との分離、中断時の下書き保持、effort未取得時の表示を確認する。問題があればfailures以下またはCodex診断情報を共有する。
