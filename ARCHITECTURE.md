# 実装の構成

アプリ本体・画面・入力処理・API通信はRustである。JavaScriptやブラウザー実行環境をアプリの実行時に使用しない。

| ファイル | 責務 |
| --- | --- |
| src/main.rs | WindowsのGUIエントリーポイント |
| src/app.rs | 画面遷移、5分セッション、照合・自己評価、設定、入出力 |
| src/model.rs | 教材モデル、TSV検証、文字列照合、内容の指紋 |
| src/scheduler.rs | 技能別の復習時期、期限順のキュー、新規上限、段階的ヒント |
| src/store.rs | 永続化、バックアップ、排他ロック、記録の整合性確認 |
| src/ink.rs | 正規化したペン軌跡の表示、PNG化 |
| src/media.rs | Windows音声合成、CPALによる録音、WAV生成・再生 |
| src/ai.rs | 任意の添削・手書き認識・音声認識のHTTPS要求 |
| data/core.tsv | 120項目の独自教材 |

UIはeframe/egui。Windowsの日本語フォントを実行時に読み込む。日本語フォントそのものは配布しない。ペン入力はポインターイベントを利用するため、機器の筆圧や傾きに依存しない。

ブロッキングHTTPと録音再生は専用スレッドで処理し、結果をチャネルでUIへ渡す。UIスレッドを待たせない。API実行中の待ち時間は学習タイマーから除く。録音はCPALの入力ストリームで最大30秒をメモリへ記録する。

API要求はHTTPSに限定し、資格情報入りURL、クエリ付きベースURL、リダイレクトを受け入れない。応答サイズ・接続時間・全体時間に上限を設ける。自動リトライはなく、送信試行回数を要求開始前に保存する。モデル名は設定可能。Responses APIの出力配列から本文を抽出し、音声認識はJSONのtextを取得する。

APIキーは起動中のメモリ、任意の環境変数、またはユーザーが明示的に選んだWindows資格情報で扱う。進捗JSONには含めない。キー保存のアカウント名は送信先別である。

データ保存はファイルロックで単一起動を保証する。書き込み失敗時は以後の学習を停止し、現在のメモリ状態をエクスポートする導線を提供する。破損したJSONを読み込んでも、既存ファイルへ既定値を自動上書きしない。

教材はIDで結合する。内容の指紋にはFNV-1a 64bitを使う。これは暗号学的な署名ではなく、日常的な教材編集の検出用である。内容が変わると該当IDの技能別Memoryを除き、次回を新規扱いにする。過去ログは保持する。

## ビルドと依存関係

Windows用依存はCargo.tomlのtarget節へ置く。コアのテストは `cargo test --lib` でGUIと分離できる。Windows上で `cargo test --all-targets` を実行すると、UI・音声・AIモジュールを含めてコンパイルする。

主要なUI・音声ライブラリは指定バージョンを使用するが、全依存の解決結果はCargo.lockに依存する。作成環境では依存取得を実行できなかったため、lockfileは未生成である。最初の成功ビルド時に保存し、その後は `--locked` を利用する。

この初版には自動アップデート、課金管理サーバー、オンライン同期、常駐通知、発音の数値採点、全教科書語彙の網羅は含まれない。

