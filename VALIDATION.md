# 確認状況とWindowsでの動作確認

確認日：2026-09-05。**配布物は未ビルドのソースであり、動作確認済みのWindowsアプリではない。** この作成環境にはRustコンパイラがなく、追加取得もできなかった。実行ファイルとCargo.lockは含まない。

## 作成環境で確認したこと

| 項目 | 結果 | 確認方法・範囲 |
| --- | --- | --- |
| 教材の列数・必須欄 | 合格 | Pythonで全120項目の15列を検査。空の必須欄なし |
| 教材ID | 合格 | 重複なし。英数字・ハイフン・下線で構成 |
| 空欄問題と解答 | 合格 | 各例文の `___` は1個。解答の各候補は空でない |
| 教材の件数 | 確認済み | 120項目・119基本語。社外メール59、技術説明49、表現・読解12 |
| Cargo.toml | 構文解析に成功 | PythonのTOMLパーサーで確認。依存解決やコンパイルを意味しない |
| Rustソースの区切り記号 | 簡易検査に合格 | 9ファイルを字句に分け、文字列・コメント以外の括弧の対応を確認。型検査ではない |
| データ保護・画面遷移 | ソースを確認・修正 | 保存失敗時の停止、復元前の退避、教材変更による該当項目だけの再学習、取り込み確認中の操作制限を確認。実機試験は未実施 |

上記の検査は教材の英文の完全性や、アプリの正常動作を保証するものではない。

## 同梱したRustテスト

テストは16件あるが、**すべて未実行**である。

- 教材モデル3件：内蔵教材の読み込み、語形を勝手に同一視しない照合、不正な取り込みの拒否。
- 復習計算6件：成功と失敗による間隔、ヒントを使った評価、時計の逆行、技能別の記録、段階的ヒント、新規上限・同じ基本語の重複導入・復習の滞留。
- 保存5件：JSON往復と遅延復習の集計、教材変更の局所的なリセット、不完全JSONの拒否、破損ファイルの保持、置換保存と二重起動の防止。
- AI接続2件：Responses形式の本文抽出、不正な送信先設定の拒否。ネットワーク接続は行わないテストである。

Windowsの `cargo test --all-targets` では16件、Windows以外の `cargo test --lib` ではコア14件を対象とする。ビルド手順はREADME.mdに記載している。

## Windows 11側で必要な確認

以下は確認手順であり、成功したという記録ではない。まずAPIキーを設定せず、通常の学習が動くことを確認する。

| 場面 | 操作 | 確認したい結果 |
| --- | --- | --- |
| 初回ビルド | `build.cmd` を実行する | テストとリリースビルドが成功し、`target\release\wordweave5.exe` が生成される。生成したCargo.lockを保持する |
| 初回起動 | `run.cmd` を実行する | 日本語表示が読める。120項目が表示される。学習記録の保存先が作成される |
| 5分の学習 | 導入、キーボード回答、照合、自己評価を進める | 目標時間後に現在の問題を終えると終了する。答えを入力しない場合、独力の成功は選べない |
| 一時停止と2分コース | 一時停止・再開、別画面への移動、2分コースを試す | 一時停止中・別画面・非アクティブ時・AI待機中は学習タイマーが増えない |
| 再起動と保存 | 数問を評価して終了し、再び起動する | 回答履歴・設定・復習時期が残る。未回答の現在位置は復元せず、出題候補を再計算する |
| 手書き | ペンタブレットで書き、一画戻す・消去・照合を試す | ペン軌跡を表示でき、照合後は元の回答を編集できない。無料の自己評価ができる |
| 読み上げ | 英語音声を選び、語句・例文を再生する | 英語が聞こえる。通常／低速の切り替えが働く。音声がない場合は対処を表示する |
| 録音 | 録音・停止・再生、30秒までの録音を試す | 声を聞き直せる。録音が30秒で停止する。マイクなし・権限なしでは説明を表示する |
| 任意のAI接続 | 利用設定済みのAPIキーで、文字起こしと添削を各1回試す | 押した処理だけ送信する。認識結果を修正できる。AIの結果が自己評価を自動変更しない |
| AI接続失敗 | 有効なキーを入力せずに送信操作を試す。回数上限を0にして再度試す | 必要な設定や上限を表示する。自動再送せず、オフラインの学習は続けられる |
| 教材編集 | TSVを書き出し、1項目だけ変更して取り込む | 変更件数を確認できる。変更した項目だけを再学習にする。キャンセル時は内容が変わらない |
| 記録の復元 | JSONを書き出してから復元操作を行う | 復元前の記録を退避する。APIキーは含まれず、当日のAI送信回数は減らない |
| 二重起動 | アプリを開いたままもう一度起動する | 2つ目は学習記録を書き換えず、先に起動したアプリを閉じるよう表示する |

記録破損の回復確認を行う場合は、テスト用Windowsユーザーを使用し、教材TSV・学習記録JSONを先に書き出す。本番の学習記録を壊して試す必要はない。

## 未確認事項

Rustの型検査、依存ライブラリの組み合わせ、WindowsのGUI・日本語フォント・資格情報・音声合成・録音・ペンタブレット、実API接続、長期間の学習効果は未確認である。最初のビルドで修正が必要な可能性がある。ビルド失敗時は `build.log` に詳細を残す。
