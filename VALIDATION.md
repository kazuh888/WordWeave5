# 現行版 0.4.0 の検証状況

2026-09-17のWindows上のコマンド、実測結果、ログ、未検証事項は [validation/harness-0.4.0.md](validation/harness-0.4.0.md) に記録する。以下は配布当時の履歴であり、最新ソースの合否として引用しない。

---

# 0.3.8の検証状況（配布当時）

教材案の作成・確認・編集・登録、追加と訂正の分離、同じ語の別用法選択、元の会話への参照を追加した。

追加した単体テスト6件（未実行）：選択した往復だけの送信対象化、重複除去と既存説明・復習状態の保護、訂正時の復習更新と追加モードでの改変拒否、補足だけの変更も含む競合検査、新規別用法の明示と二重登録拒否、未完成案と会話参照のシリアライズ。旧データ移行テストにも追加項目がない場合を含めた。

この環境にはcargo/rustcとWindows実行環境がなく、コンパイル・テスト実行・UI・実Codex応答は未確認である。既存の通信テスト14件（Windows）を維持したが、合格したという主張ではない。実施したのはソース差分、TOML、依存関係・教材の不変性、ZIP整合性の静的確認である。

生成は既存のentry_schemaとcodex::generateを利用する。追加した教材生成プロンプトが常に意味的に正しい教材を返すことは保証しない。教材は確認ボタンを押すまで反映しない。追加時の重複判定は正規化文字列比較であり、意味的重複や誤文の判定は確認対象である。

保存は既存のcustom.tsvとprogress.jsonの2ファイルであり、単一トランザクションではない。教材保存後に学習記録の保存が失敗した場合は停止し、会話参照をメモリに保持してエクスポートによる復旧を可能にする。両ファイルを完全に同時更新できるとは扱わない。

---

# 0.3.7の検証状況（以下は配布時点の旧記録）

モデル・effortの表示、会話履歴と下書き・メモの保存、限定したコンテキストを再送する英語チャットを追加した。

追加テスト（未実行）：

- 単体7件：取得済み実行設定の保持、effort欠落/null時の未取得表示、文脈の時系列・参照指定・容量制限、参照指定が大きすぎる場合の停止、失敗した回答を保存しないこと、会話間の分離、Storageを閉じて開き直した後の履歴・下書き・実行設定の保持。
- 通信3件：要求モデルと異なる返却モデル・effortの取得、effort欠落/null、2回目の質問で再読み込みした履歴が実際に模擬プロセスへ渡ること。
- 既存のデータ互換テストでchatsを持たない旧版記録の読み込みを追加確認する。

Windowsの通信テストは14件、非Windowsでは12件の構成である。これらは実行済みの件数ではない。この環境ではcargo/rustcが見つからず、コンパイル・テスト実行・Windows画面・実Volta/Codex接続は未確認。静的な差分・TOML・ロックファイル・教材の不変性・ZIP整合性のみ確認した。

送信量はバイト数によるアプリ独自の上限であり、LLMのトークン容量や回答品質を保証しない。履歴の検索・参照指定・メモ編集は利用者が操作する。履歴の自動要約は含まない。

---

# 0.3.6の検証状況（以下は配布時点の旧記録）

0.3.5の通信テスト10件は利用者のWindowsで合格した。しかし実アプリではcmd.exe経由の起動に戻り、initializeの応答前に終了コード1となった。同一フォルダーの模擬Voltaでの成功は、実環境の配置を網羅していなかった。

0.3.6では、既知のVolta shimを識別し、同一フォルダーに本体がない場合にPATHを検索する。PATHにもない場合は、通信や認証の失敗ではなく、起動前の探索失敗を返す。

追加した確認項目：

- 単体テスト4件：別フォルダーからの探索、同一フォルダーの優先、他形式のwrapperをPATH上のVoltaで置き換えないこと、Volta不在時の診断。
- Windows通信テスト1件：日本語・空白を含む別々のディレクトリにshimと模擬Voltaを配置し、PATH経由で起動する。引数・作業ディレクトリ・initializeから生成完了までの標準入出力通信・起動経路の診断を確認する。環境変数は専用の子テストプロセスにだけ渡し、通常の並列テストと干渉させない。
- 既存のWindows通信テスト10件を維持する。Windowsでは合計11件、非Windowsでは9件である。

この環境にはcargo/rustcとWindows実行環境がなく、追加テストは未実行である。コンパイル・実Volta・ChatGPT接続も未確認。ソース差分、TOML、ロックファイルの依存関係、教材ファイル、ZIP整合性のみ静的に確認する。

一般的なcmd/bat向けCALL経路は従来のままである。その経路の問題解消や、子孫プロセスを含む完全な終了回収を今回の修正の成果とはしない。

---

# 0.3.5の検証状況（以下は配布時点の旧記録）

利用者のWindows実行結果：0.3.4は通信テスト10件中9件合格。`CALL`方式でも、テスト用codex.cmdは模擬EXEへ到達せず、cmd.exeが終了コード1で終了した。

利用環境で確認されたcodex.cmdは `@echo off` / `volta run %~n0 %*` のVolta shimである。0.3.5ではcodex.cmdと同じフォルダーにvolta.exeがある場合、cmd.exeとcodex.cmdを介さず、`volta.exe run codex app-server`を直接起動する。この経路のWindows統合テストでは、模擬volta.exeが正しい3引数を受け、標準入出力JSON-RPCを維持することを検査する。

Volta以外のcmd/bat向けCALL経路は残すが、今回のテスト対象と利用環境の主経路ではない。テスト名もVolta shimの検査であることが分かる名称へ変更した。テスト数10件と合格条件は維持した。

この環境にはcargoとWindows実行環境がなく、0.3.5は未コンパイル・未実行。静的整合性のみ確認し、Windowsでの10件合格はまだ主張しない。

---

# 0.3.4の検証状況（以下は旧版の記録）

利用者のWindows実行結果：0.3.3の通信テスト10件中9件合格。残るWindowsバッチテストは、模擬EXEへ到達せず、cmd.exeが終了コード1で終了した。stderrは2行とも未分類だった。

0.3.4では、`/C`のコマンド文字列の先頭を引用符付きパスにする方式から、`CALL "バッチパス" app-server`へ変更した。コマンド文字列全体の外側の引用符と、パスの内側の引用符は維持する。危険文字の拒否も維持した。テスト条件は緩和していない。

この環境にはcargoとWindows実行環境がなく、0.3.4は未コンパイル・未実行。静的整合性のみ確認し、Windowsでの10件合格はまだ主張しない。

---

# 0.3.3の検証状況（以下は旧版の記録）

利用者のWindows実行結果：0.3.2の通信テスト10件中8件合格、2件失敗。初期終了時の終了コード未確定と、バッチ経由通信の失敗を確認した。

0.3.3ではEOF後の終了状態を最大2秒ポーリングする。stderrのEOFだけではプロセスの終了を確定しない。
終了未確定を成功として扱わず、テストで終了コード7を要求する条件も維持した。
バッチ起動テストは失敗時にその接続固有の診断情報と、模擬EXE開始・引数確認・作業ディレクトリ確認・JSON受信の段階を表示する。
2件目の根本原因は現時点では未確定。引用符を再度推測で変更せず、現象を区別できる情報を増やした。

この環境にはcargoとWindows実行環境がない。0.3.3のコンパイル・テスト実行は未実施。
静的な差分・TOML・依存関係・ZIP整合性確認のみ。テスト10件が合格したという主張はしない。

---

# 0.3.2の検証状況（以下は旧版の記録）

今回の作業環境ではcargo/rustcが利用できず、コンパイル・テスト実行・Windows動作・実Codex接続は未確認。
実施したのはソース差分、TOML構文・版番号、教材データの不変性、ZIP整合性の静的検査のみ。
「修正済み」はソース変更済みの意味であり、現象解消を実機で確認したという意味ではない。

追加したテスト：診断リングの上限、stderr分類に機密文字列を含めないこと、バッチ引用符と危険文字拒否、未認証、初期終了、JSON不正、Windows上の日本語/空白パスのバッチ経由通信。

Windowsで `cargo test --all-targets --locked` を実行すること。通信統合テストはWindowsでは10件、非Windowsでは9件を登録する構成。
Windows専用テストを含むので、Linuxだけの成功をWindows検証済みとはしない。

制約：診断記録は直近1回・最大128件でメモリ内のみ。大量のstderrがあると古いイベントは消える。
標準エラーはUTF-8として分類し、元の文章は残さない。Windowsの別文字コードや未知のエラーは未分類になり得る。
終了コードは起動した直接のプロセスのもので、Volta等の子孫プロセスの終了コードとは限らない。
バッチを介する場合、既存の中断処理が子孫プロセスまで回収することは保証していない。
診断だけでは未知の原因を必ず特定できるわけではない。認証情報ファイルや認証URLの共有は求めない。

---

# 0.3.1の検証状況（以下は旧版の記録）

確認日：2026-09-05。

## 今回確認したこと

- 通信テストファイル全体の `#![cfg(unix)]`、Unix固有の実行権限設定、Pythonスクリプトを撤去した。
- Cargo.tomlにRust製模擬Codexのバイナリターゲットを追加し、統合テストから `CARGO_BIN_EXE_wordweave-mock-codex` で参照する。
- 通信テスト6件を維持した。APIキー認証・音声非対応時は生成要求が送られていないこともログで確認する。
- 中断テストは待機状態の通知後に中断し、3秒以内の復帰と、子プロセスの保持するOSファイルロックが解放されたことを検査する。起動待ち上限は15秒。
- テスト作業ディレクトリは各試験で分離し、日本語と空白を含むパスを使用する。
- Cargo.toml/Cargo.lockの版番号整合と、配布内容・変更範囲を静的確認した。
- アプリ本体のsrc配下と教材データは0.3.0と同一である。

## 未実施

今回の環境ではcargo/rustcが見つからず、Rust公式配布先への接続もタイムアウトした。**0.3.1のコンパイル、Linux/Windowsでのテスト実行、Windows実機動作は未確認である。29件合格とは主張しない。**

以下の旧版の合格記録は、今回の修正版の実行結果ではない。

## Windowsで実行する確認

```powershell
cargo test --test codex_process --locked
cargo test --all-targets --locked
cargo build --release --locked --bin wordweave5
```

最初のコマンドで通信テスト6件が列挙・実行され、`6 passed; 0 failed` となることを確認する。全対象では共通処理23件＋通信6件を実行する構成である。main.rsや試験補助バイナリのテストハーネスに表示される0件は問題ない。

テストは模擬Codexとのローカル標準入出力通信のみ。実際のCodexやChatGPTには接続しない。Cargoの依存関係取得時はネットワーク接続が必要になる場合がある。

---

# 0.3.0の検証結果（旧版の記録）

確認日：2026-09-05。作成環境はLinux、Rust 1.98.1、Windowsチェック用ターゲットはx86_64-pc-windows-msvc。

## 実施した検証

| 検証 | 結果 | 範囲 |
| --- | --- | --- |
| 共通Rustテスト | 23件合格 | 教材・CSV・復習・保存・旧形式互換・Codexイベント処理 |
| モック子プロセスとの通信 | 6件合格 | 分割されたJSON行、開始応答より前の完了通知、APIキー認証の拒否、失敗したターンの部分出力拒否、中断と子プロセス回収、音声対応/非対応の分岐 |
| Windows向けcargo check --all-targets | 合格・警告なし | 画面・AI接続・手書き・音声を含むWindowsコードとテストの型検査。リンク・実行は含まない |
| 公式NGSL取得と解析 | 合格 | 元CSV2件を取得。頻度順2,809語、補足52語、重複除外後2,859語。配布する候補リストとの一致をRustで検証 |
| 教材拡張の保存・再読み込み | 合格 | 旧15列/新17列、言い換え・例文のJSON配列、重複拒否、追加例文では既存の成績を初期化しない |
| API課金経路 | ソースとモックで確認 | 旧Responses/音声API呼び出しを撤去。app-serverのChatGPT認証のみ使用し、apiKey認証はターン開始前に拒否 |
| Codex通信形式 | 公式資料とローカルスキーマで照合 | initialize、account/read、thread/start、turn/start、画像/音声入力、モデルの入力対応状況、完了通知 |

実行コマンド：

```text
cargo test --all-targets --locked
cargo check --all-targets --locked --target x86_64-pc-windows-msvc
```

`tests/codex_process.rs` はUnix/Python3のモック試験であり、Windowsでは対象外。Windowsの `cargo test --all-targets --locked` では共通テスト23件を実行し、Windowsのアプリコードもコンパイルする。

## 未確認の範囲

- Windowsでのreleaseリンク、EXE起動、画面操作、フォント、ペンタブレット、読み上げ、録音再生。
- 利用者のChatGPT認証を使った本番Codexへの生成・添削・画像/音声送信。
- Windows上でのNGSL再ダウンロード、プロキシ・証明書環境ごとの接続。
- 全2,859語の教材生成や、生成英文の言語的な正確さ・学習効果。

実行ファイルは同梱しない。検証に使った依存解決結果のCargo.lockを同梱する。

## Windowsで最初に試す操作

1. 旧アプリを終了し、学習記録と教材をバックアップする。
2. 新しいフォルダーで `build.cmd` を実行し、`run.cmd` で起動する。
3. 旧記録が残り、136件の内蔵教材と追加教材を表示できることを確認する。
4. `codex login` 後、設定の「接続・ChatGPT認証を確認」を押す。
5. 1回の語数を1語にし、提供語 `investigate` を自動生成・登録する。言い換えの条件と6例文を確認する。
6. 同じ語で「英文追加」を押し、例文が増えることを確認する。
7. 日本文「問題の原因を調査しています。」を登録し、原文・英文・解説が保存されることを確認する。
8. NGSLの自動生成を少数で開始し、中断→再起動→未処理から再開を確認する。
9. 教材TSVと学習記録JSONを書き出し、新しい言い換え・例文も含まれることを確認する。

音声入力は、対応するCodex CLIとモデルの場合だけ利用可能。未対応なら説明を表示する。画像・音声機能の失敗をAPIへ自動転送して補う処理はない。
