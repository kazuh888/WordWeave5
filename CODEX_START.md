# Codexへの移行：最初に読む資料

## 2026-09-17 更新

現在の開発版は **0.4.0** である。[UPGRADE-0.4.0.md](UPGRADE-0.4.0.md)、[Harness検証記録](validation/harness-0.4.0.md)、[tasks/todo.md](tasks/todo.md) を先に読むこと。以下の0.3.8移行文とHANDOFF_MANIFEST.jsonは当時の履歴であり、現在のソースや合格の証拠ではない。既存のローカル変更を保持し、未完了の検証から続ける。AIはChatGPT認証のapp-serverのみとする方針は継続する。

---

作成日：2026-09-09。引き継ぐソースはWordWeave5 0.3.8である。
このパッケージは元の0.3.8ソースを変更せず、引き継ぎ資料を追加したものである。実行ファイル・ユーザーの学習記録・Codex認証情報は含まない。

## 移行手順

1. ZIPを新しいフォルダーに展開する。`wordweave5`がプロジェクトルートであり、ここに`Cargo.toml`がある。
2. 手元でソースを修正している場合、上書きせず、この版との差分を先に確認する。
3. WordWeaveを終了して、`%LOCALAPPDATA%\WordWeave5`を別の場所へバックアップする。教材と学習記録の両方が必要である。ソースを別フォルダーにしても通常のアプリ保存先は同じである。
4. Codexでこのプロジェクトフォルダーを作業対象にし、以下の開始文を渡す。
5. 最初にビルドとテストを確認し、次にチャット・教材化を少量で実機確認する。詳細は`NEXT_STEPS.md`にある。

このChatGPTの会話履歴が移行先へ自動的に渡ることを前提にしない。必要な決定・経緯は`HANDOFF.md`にまとめた。認証情報を引き継ぎ文に貼る必要はない。

## Codexに渡す開始文

```text
WordWeave5の開発をこのフォルダーで引き継いでください。
まずCODEX_START.md、HANDOFF.md、NEXT_STEPS.md、UPGRADE-0.3.7.md、UPGRADE-0.3.8.mdと実装を読んでください。

最新版は0.3.8です。0.3.7のモデル・effort表示／履歴付き英語チャットと、0.3.8のチャット教材化は、ソース実装済みですがビルド・テスト実行・Windows実機・実Codex接続が未確認です。古いvalidationログを最新版の合格証拠にしないでください。

最初の作業は現在のソースでビルドとテストを成立させることです。利用可能な環境を調べ、Windowsならcargo test --all-targets --lockedとcargo build --release --locked --bin wordweave5を実行し、失敗を修正してください。非Windowsで確認できない範囲は明記してください。現状を調べる前に全面改修や依存関係の更新をしないでください。

AIはChatGPT認証のcodex app-serverだけを使用し、APIキー課金へのフォールバックは追加しないでください。Volta shimの直接起動・PATH探索修正を維持してください。モデルとeffortはサーバーから返った値を表示し、未取得値を推測しないでください。

会話コンテキストはWordWeaveが保存・選択して再送します。チャットからの教材化は選択した往復を根拠に案を作成し、利用者が差分を確認して登録する方式です。追加モードは既存内容と復習状態を維持し、訂正とは区別してください。

既存の教材・学習記録を保護し、手元の変更を上書きしないでください。修正点、実際に実行した検証と結果、未検証事項を日本語で報告してください。まずNEXT_STEPS.mdの優先度P0とP1を進めてください。
```

## 同梱資料

| 資料 | 用途 |
| --- | --- |
| HANDOFF.md | 目的、要件、構造、保存形式、障害対応の経緯、既知の制約 |
| NEXT_STEPS.md | 移行後の優先順位、実機確認と完了条件 |
| README.md / ARCHITECTURE.md | 既存の利用・設計資料 |
| UPGRADE-0.3.7.md / UPGRADE-0.3.8.md | 最近追加した機能の操作と仕様 |
| VALIDATION.md | 版ごとの検証記録。旧版と現行版を区別すること |
| HANDOFF_MANIFEST.json | 元ソースを変更していないことのSHA-256一覧 |
