# WordWeave 5 0.2.0

## 更新方法

0.1のビルド・画面表示はユーザー環境で成功した。この0.2のコンパイル・Windows実行・実API接続は未確認である。作成環境ではRustコンパイラを利用できない。

1. 旧版の設定から学習記録JSONと教材TSVを書き出し、アプリを終了する。
2. 更新版ソースを別フォルダーに展開する。旧ソースと実行ファイルは保管する。
3. 旧フォルダーのCargo.lockがあれば、新しいソースフォルダーにコピーする。依存ライブラリの指定は変更していない。ルートパッケージのバージョンが変わったためlockfileが更新される場合がある。
4. 新しいフォルダーでbuild.cmdを実行し、成功後に同じフォルダーのrun.cmdを実行する。

保存先は以前と同じ `%LOCALAPPDATA%\WordWeave5`。旧記録は読み込める。追加したwriting_logs欄は省略時に空とする。旧版へ戻す場合、旧版は新しい添削履歴を保持しないため、0.2の記録も先にエクスポートする。

## 公開語彙の取り込み

学習セッションを終了し、「語彙を追加」を開く。

1. 「公式NGSLのCSVを取得」を押す。AI料金は発生しない。
2. 候補を検索し、1語を選ぶ。
3. 「選択した語の教材案を生成」を押す。選択した1語だけを設定済みAPIへ送り、送信1回として数える。
4. 意味・社外メール表現・文体・用法・例文を確認する。「TSVを編集する」で修正できる。
5. 「内容を確認して教材へ追加」を押し、追加・変更件数を確認して確定する。

取得した候補はwords.csvとして保存し、次回起動でも読み込む。候補を取得しただけでは学習対象には入らない。教材化して確認した項目だけを追加する。AI教材案の水準は「目安未判定」とする。

取得できない場合は[NGSL公式ページ](https://www.newgeneralservicelist.com/new-general-service-list)から「NGSL 1.2 alphabetized and lemmatized for teaching」を保存して、「保存済みCSV・単語リストを読み込む」で開く。自分の単語一覧も使える。UTF-8、先頭列が基本語、または数値順位・基本語の順。1行1語のテキストにも対応する。複数行セルは未対応。

作成環境ではCSVの取得が通信承認の未完了により停止したため、公式CSV自体の取得・解析成功は未確認である。CSV形式が変わった場合は上記の列構成に整える。

出典：New General Service List / Browne, C., Culligan, B., and Phillips, J. / CC BY-SA 4.0。利用条件は公式ページを参照する。取得したリストの再配布には元のライセンスが適用される。このソース配布にはNGSLの全語彙データそのものは含まれない。NGSLは一般英語の語彙リストであり、日本の教科書への収録・学年を保証しない。

## 熟語・構文

独自教材を各8項目追加した。設定の対象分野で「熟語」「構文」を選べる。

- 熟語：look into、carry out、in addition to、due to、look forward to、get back to、as soon as、in charge of。
- 構文：Could you ...?、We would appreciate it if ...、It is necessary to ...、so that、unless、not ... until、allow someone to ...、whether節。

「語句・綴り」は空欄、「使い分け」は用法、「日本語から英作文」は構文を使った文全体を練習する。構文名の「主語」「動詞の原形」「...」は型を示す記号であり、そのまま英文へ入力しない。

教材追加時は、熟語のIDをphrase_、構文のIDをpattern_で始める。構文のbase欄に型を記載する。既存の15列TSV形式は変更していない。

## 日本語出題・添削・書き直し

1. 設定で「日本語から英作文」を有効にする。この練習だけを試す場合は他の練習を外す。通常の新規上限・復習期限が適用される。
2. 学習を開始し、初めての教材では導入を確認する。
3. 初めは教材の日本語訳を表示する。回答前に「新しい日本語の問題を生成」を押すと、その対象表現に合う別の場面の問題をAIが生成する。
4. 英文を入力する。手書き・音声の文字起こしも使える。誤認識は修正してから照合する。
5. 「回答を照合」で模範例を確認し、「AIに使い方を確認する」で添削を受ける。
6. 必要なら「添削を踏まえて書き直す」で修正し、再度送信する。
7. 自己評価を選んで復習記録を確定する。書き直し後はヒントありとして扱う。

添削は「意味の一致」「文法」「自然さ・語調」「対象表現の使用達成」を分ける。自然な別解を認め、AIによる自動採点・復習間隔の自動変更は行わない。

「記録」に最新20件の問題・回答・添削と初回/書き直しの区別を表示する。履歴全体はprogress.jsonとそのエクスポートに含まれる。終了時の未回答問題は次回起動で復元しない。添削が返る前にアプリを終了した場合、その添削は保存されない。

問題生成・添削・再添削は各1回のAPI送信であり、料金と送信上限が適用される。待ち時間は学習タイマーから除く。生成に失敗しても教材の日本語訳で続けられる。

## app-server接続

今回の追加機能は既存のHTTPS API接続を使用する。app-server接続アダプターは未実装である。

公開仕様で確認できるのは `codex app-server`。Rustの子プロセスとして起動し、標準入出力のJSONLでinitialize → initialized → thread/start → turn/startと通信し、完了通知から出力を取得する。ChatGPTアカウントのログインも仕様に含まれる。

`ChatGPT.exe appserver` が同じプロトコルを公開しているかは未確認である。実行ファイル・引数の確認後に接続方式を確定する。公式ログインを利用し、認証情報を手動抽出する方式にはしない。文章生成に対応していても、音声認識・画像入力・利用可能モデル・利用枠が既存APIとすべて同じとは限らない。

参考：[公式App Server仕様](https://learn.chatgpt.com/docs/app-server)、[Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs)。確認日2026-09-05。

## 検証状況

教材136項目の15列・必須欄・ID重複・空欄問題の構造と、Rustソースの字句上の括弧対応は静的に検査した。これはコンパイルや動作試験を代替するものではない。

追加した4件のRustテストは、CSVの解析、出題対象の一致、熟語・構文データの整合性、AI生成IDが無関係な既存カードを上書きしないことを対象とする。全体で20件（Windows上）あるが、作成環境では未実行である。

実機では、旧記録の読み込み、語彙CSVの取得/ローカル取り込み、教材案の編集/キャンセル/追加、日本語生成の成功/失敗、添削/書き直し、再起動後の添削履歴を確認する。今回の更新版のビルド・GUI・ネットワーク動作は未確認である。
