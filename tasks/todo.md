# Windows Harness タスク

## 設定画面の残件（SETTINGS-UI-001、2026-09-26）

計画：[保存方式と連続ズーム](plan.md)。追加依頼によりA→B→Cを実装・自動検証済み。利用者の実操作受入と従来の未完了実機受入は保持する。

- [x] 用語統一・③のファイル自動判定/教材取り込み統合：全280テスト・release・隔離描画成功後、利用者が「OK！」と回答。commit/pushは未実施。
- [x] 設定の保存方式を利用者が決定：「保存／キャンセル」、設定の自動保存なし。

### A：設定の保存／キャンセル（実装・検証済み）

- [x] 編集用設定を確定設定から分離し、全分類を「保存」で確定、「キャンセル」で最後の保存値へ戻せる。
- [x] 定期保存・終了保存・学習/教材/チャット保存のいずれでも未保存設定を保存しない。学習記録とチャットの既存自動保存は維持する。
- [x] 保存失敗を成功表示せず、確定済みデータと編集値を保持して再試行・取消しできる。

検証：隔離ストレージの読み直しによる未保存値の非混入、保存成功/失敗、取消しの回帰試験。全対象テスト・releaseビルド。依存：なし。想定規模：M（3～5ファイル）。対象候補：`src/app.rs`、`src/app/settings_ui.rs`、設定編集状態の小モジュール、対応テスト。

### B：設定試用・離脱・復元との整合（実装・自動検証済み、実音声等は未受入）

- [x] 接続確認・音声試聴が編集値を使う場合も、それだけでは設定を保存しない。AI認証方式・PATH探索・返却モデル表示を維持する。
- [x] 未保存の画面離脱/終了、プレビュー後の取消しを整理し、復元後に古い編集値を再保存しない。
- [x] 設定画面外の音量/リピート/倍率との同期方針を明示し、既受入の操作結果や学習中の入力を不用意に失わない。

検証：モック接続、音声設定の状態試験、離脱/復元/取消しの回帰試験とWindows表示確認。実音声受入は別記する。依存：A。想定規模：M（3～5ファイル）。対象候補：`src/app/settings_ui.rs`、`src/app/control_settings.rs`、`src/app/playback_panel.rs`、`src/app/backup_ui.rs`、対応テスト。

### チェックポイント：保存方式

- [x] 保存境界の独立確認、`cargo test --all-targets --locked`、`cargo build --release --locked --bin wordweave5` が成功する（全285テスト）。
- [x] 利用者が保存／キャンセルと未保存状態の扱いを確認する。

### C：操作部を固定した連続ズーム（実装・自動検証済み、実マウス受入待ち）

- [x] 倍率スライダーの位置・大きさを操作中に固定し、ドラッグ中も連続して表示へ反映する。座標固定と連続ポインター追従を回帰試験で確認した。
- [x] 50～160%、標準80%、既存のCtrl＋ホイール/キーと大・標準・小を維持し、連続プレビューを保存／キャンセルの境界と整合させる。
- [x] 標準/狭画面で本文スクロール、設定保存/取消し、ナビゲーションの配置をWindows描画で確認した。反復拡縮の座標安定は自動試験済み、実マウス・高DPI/タッチの受入は下記チェックポイントに残す。

検証：倍率変更中の物理座標/サイズ安定と連続入力の回帰試験、Windows実描画・実マウス操作。全対象テスト・releaseビルド。依存：A・B。想定規模：M（3～5ファイル）。対象候補：`src/app/chrome.rs`、`src/app/settings_ui.rs`、`src/app/visual_check.rs`、対応テスト。

### チェックポイント：今回の残件完了

- [x] A/B/Cの検証結果と残る実機未検証を記録し、利用者が保存方式・倍率操作を受入確認する。全体実機確認は別途実施する方針を受領。
- [x] バックアップ復元を隔離データで最終確認する。取消し・復元・再起動・原本保持・破損拒否と全286テスト、release成功。
- [ ] 承認済みのcommit・push・PR作成・CI成功確認・mainマージを実施する。公開結果はGitHub PRを正とする。

## UX改善0.6.0（2026-09-21）

仕様: [承認済みUX計画](../docs/design/ux-completion.md)。操作と実機受入: [UPGRADE-0.6.0](../UPGRADE-0.6.0.md)。

- [x] 学習入力・IME安全な照合・明示評価、ホームからの開始／再開、終了結果を実装する。
- [x] 教材・語彙追加・チャット・記録・設定を用途別に整理し、共通UIと狭幅表示を追加する。
- [x] 未取得のAI実行設定・接続状態・習得度を推測しない表示とする。
- [x] 最終レビュー指摘1件を解消し、Windows全198件テスト・releaseビルド・Clippyを実行した。[検証記録](../validation/ux-0.6.0.md)に結果と警告・未検証事項を残す。
- [ ] 保全済み実記録での起動・再起動・バックアップ復元を確認する。
- [ ] 5分の学習一巡、最小820×650・文字倍率80～160%、実IME・マイク・ペン・TTS・実Codexを受入確認する。

下の旧版合格を0.6.0の実機受入に代用しない。実装・自動試験と利用者によるUX受入は区別する。

## PATH補完0.5.1（2026-09-20）

- [x] 保存システム・実行ユーザーPATHを補完し、CodexとVoltaで共有する。明示パス欠損時は停止する。
- [x] 修正前の再現失敗とオフラインコンパイルを確認する。
- [x] Windows全167件テスト、releaseビルド、起動時PATHが空の状態での保存PATH探索、独立レビュー。[記録](../validation/path-search-0.5.1.md)。

## 操作制御0.5.0（2026-09-20）

仕様: [操作制御](../docs/design/interaction-controls.md)。以下は新規変更の検証であり、下の0.4.0合格とは別である。

- [x] diagnostics: 永続・容量制限・秘密非保存・保存失敗の分離、書き込み中断を試験。
- [x] effort: model/list、設定保存、生成時指定、返却値表示をモック通信試験。
- [x] chat-trash: ごみ箱移動・復元・原本と根拠保持・保存失敗・復元後readonlyを試験。添付名を変更。
- [x] recording-controls: 一時停止時間を除外、キャンセル確認と既存録音保護を実装。純粋ロジックと保存失敗を試験。
- [x] speech-controls: ±5秒、pause/resume/stop、0.5〜4倍と位置表示を統合。境界判定・Windows無音API試験。
- [x] Windows全159件テスト・releaseビルド成功、差分レビューと更新手順。[検証記録](../validation/controls-0.5.0.md)。
- [ ] 実機確認: マイク、TTSの速度/音質、CLI0.153.4実生成のeffort反映。

## 0.4.0までの履歴

- [x] 基準検証：変更前のWindows全テスト76件成功。現行版の証拠とは分ける。
- [x] media-assets：原本保存・検証・バックアップ。新規モジュールと一時フォルダー試験。
- [x] run-journal：生成前保存、応答保存、結果不明・exact turn再読込。再生成せず応答を回収して保存する。
- [x] chat-input：録音、認識候補の確認、注釈、添付履歴・再生・読み上げ。UI・AI・会話の互換試験。実機確認は下記。
- [x] material-review：左右差分、理由hover、固定根拠と引用強調、登録前検査。教材モジュール・比較UI試験。
- [x] 保存統合：媒体を含む退避・復元、教材登録失敗時の回復、旧データ保護。
- [x] 最終自動検証：Windows全119件テスト成功、releaseビルド成功、限定再レビュー、ソース・EXE指紋、検証記録と操作手順。
- [ ] 利用者の実機評価：実Codexの媒体入力・結果再取得、マイク・原音再生・TTS停止、Windowsペン、比較画面のUX、保全済み実記録での更新確認。Androidは対象外。

現在の証拠と未検証範囲は [validation/harness-0.4.0.md](../validation/harness-0.4.0.md) を参照する。マイク／ペン／実Codexは実装完了と区別して扱う。
