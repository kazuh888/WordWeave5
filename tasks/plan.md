# Windows Harness 実装計画

## 設定画面の残件（SETTINGS-UI-001、2026-09-26）

2026-09-26の追加実装依頼によりA/B/Cを実装し、全285テスト・release・Windows隔離描画・保存境界の独立確認を完了した。利用者による実マウス/音声等の受入は残る。進捗の正本は [タスク一覧](todo.md) の同ID節とする。

### 確定した要求

- 設定は「保存」「キャンセル」方式とし、未保存の編集値を自動保存しない。保存成功時にだけ確定し、キャンセルは最後に保存した値へ戻す。
- Excelの参考動画のように、倍率操作部の位置・大きさを固定し、ドラッグ中も表示へ連続反映する。従来のアプリ全体を拡縮する範囲、Ctrl＋ホイール/キー、大・標準・小、起動時80%は維持する。
- 用語統一と③のファイル自動判定・教材取り込み統合は利用者受入済みであり、やり直さない。

### 実装順と境界

1. **A：設定の編集用データと保存／キャンセル。** 保存済みSettingsと編集中Settingsを分離する。学習記録・チャットの自動保存や他機能の直接保存から、編集中の設定が漏れて保存されないようにする。
2. **B：試用・画面移動・復元との整合。** 接続確認や音声試聴に使う編集中の値を明示し、試用を永続化と混同しない。保存失敗・取消し・復元後に古い下書きが確定データを上書きしない。AとBをまとめて保存境界の独立確認と利用者確認を行う。
3. **C：操作部を固定した連続ズーム。** A/Bの保存境界を使い、倍率操作を描画倍率の影響から分離する。倍率の連続プレビューと保存・取消しを両立させる。スライダーを離してから一度だけ適用する現行の暫定対処を置き換える。

### 実装した境界

- 未保存の離脱／終了は「保存して進む／破棄して進む／編集を続ける」。保存失敗時は移動せず、編集値を保持して再試行できる。
- 外部再生パネルの音量/リピート記憶は維持し、編集開始時の値・編集値・最新の確定値を項目別に比較して統合する。設定画面内の倍率ショートカット/プリセットは下書きへの変更、設定画面外は従来どおりである。
- 接続確認/候補取得/音声試聴は編集値を使用するが保存しない。取消しは設定の試聴を停止し、表示倍率を確定値へ戻す。試聴中の再生速度変更も下書きだけに反映する。復元確認では未保存設定の破棄を明示し、成功後に古い下書きを初期化する。
- 倍率操作は常に同じIDを持つ固定位置Areaで行い、サイズと文字を倍率から独立させる。egui 0.31の画面だけの倍率換算を補い、次フレーム入力のポインター/タッチ座標も換算する。

### 検証・対象外

隔離データで、定期保存・終了保存・教材/チャット保存中の未保存設定保護、保存成功/失敗、取消し、復元後同期を検証する。連続ズームは50～160%で操作部の物理座標/サイズ、ドラッグ追従、文字・ボタンの見切れを確認する。各完結した変更後に全対象テストとreleaseビルド、標準/狭画面のWindows描画を行い、実操作の利用者受入は別に記録する。実データを使う破壊的試験、GitHub公開・CI設定変更、無関係な全体整形は含めない。

## PATH探索の補完（2026-09-20）

0.5.1として実装・全167件テスト・releaseビルド・実探索確認・独立レビューを完了した。[検証記録](../validation/path-search-0.5.1.md)。CLIの更新と実生成は実施していない。

利用者の追加要求: 起動時PATHに加え、Windowsの保存されたシステムPATHと実行ユーザーのPATHを読み取り、自動検出に使う。順序は起動時→システム→ユーザー、各PATH内の順序は保持する。Volta本体にも同じ探索範囲を使い、shim直接起動を維持する。空・相対PATHは作業フォルダーを誤起動しないよう除外する。明示パス失効時の暗黙代替、設定・環境変数・レジストリの書き換え、CLIの更新、実生成は行わない。再現テスト→実装→全テスト・releaseビルド→読み取り専用の実環境探索確認を実施する。

## 操作制御の追加

2026-09-20: 0.5.0の実装・独立レビュー・全159件テスト・releaseビルドを完了。結果は [検証記録](../validation/controls-0.5.0.md)。実音声・マイク・CLI0.153.4の実生成は未確認であり、次は [操作手順](../UPGRADE-0.5.0.md) に沿った利用者評価である。

2026-09-19再開。[操作制御の追加仕様](../docs/design/interaction-controls.md)を現行差分の契約とする。診断、effort、音声backendは独立して実装し、設定・録音・再生UI・チャットごみ箱を統合する。各スライスの失敗テスト→実装→成功テスト後、Windows全体検証を行う。以下の原本保護・AI接続方針と未完了の実機確認は維持する。

## 目的と前提
Windowsの英語チャットで音声入力・読み上げ・筆跡注釈を扱い、原本を残しながら教材案を理解しやすく比較・承認できるようにする。利用者から前回提案の設計・実装指示を受けた。Androidは今回考慮しない。

## 設計契約
- 元の録音、元の筆跡、注釈の対象となる英文、実際に送信するPNG、文字起こし結果と訂正文を区別する。録音は既存の30秒上限を維持する。
- 添付は独立ファイル。Progressには参照・説明のみを追加し、旧JSONをserde defaultで読める。原本を自動削除しない。ファイル長とハッシュを検査し、保存失敗時に保存済みと表示しない。
- 手書き文字認識とは別に英文注釈面を用意する。英文と描画座標を固定し、送信前の画像プレビューを提供する。取消線は削除の承認ではない。
- 認識は自動送信しない。元の会話・下書きへだけ結果を反映し、途中で変更された下書きを上書きしない。Codexへ送る媒体を利用者に明示する。
- 会話コンテキストの選択はWordWeaveに残す。過去の原本を保存していても毎回すべて送信しない。教材化は選択した往復の固定版を根拠とする。
- 比較は構造（項目・例文・言い換え）で整列し、変更箇所を文字単位で強調。理由と引用はAIが返した情報と照合し、未取得や利用者編集後に推測で補わない。根拠は隣に表示し、引用一致部分だけ強調する。
- 新規・追加・訂正・削除の承認境界と復習記録保護を維持する。生成結果がそのまま教材ファイルを書き換えることはない。
- 実行前に意図を保存し、応答を検査前にも回収可能な形で保存する。未完了で再起動した処理は結果不明。通信切断・待機終了をサーバー中断確認にしない。再保存と再生成を区別し、自動再生成しない。
- ChatGPT認証のapp-serverだけを使い、既存CLI設定・Volta/PATH探索・返却モデル/effortのみの表示を保持する。
- 二つの既存保存ファイルの更新について保存途中の失敗を成功扱いしない。根拠・媒体を含むバックアップ手段を用意する。

## 構造・規約
`src/`はRust本体、`src/app/`はWindows egui画面、`tests/codex_process.rs`は実APIを呼ばない子プロセス試験。新規の純粋ロジックはlibモジュールに分離し、画面を巨大化させない。既存の`Result<T, String>`とserde形式を踏襲する。

```rust
// 保存が成功するまで、表示中の確定データを置き換えない。
storage.save(&next)?;
*progress = next;
```

## 検証
基本コマンド：`cargo test --all-targets --locked`、`cargo build --release --locked --bin wordweave5`。
各スライスで失敗するテストを先に追加し、既存記録の互換性、壊れた添付、範囲外座標、根拠不一致、同一操作の再反映、保存失敗を検査する。テストは専用一時フォルダーを用い、実学習データを変更しない。GUIは可能な環境で実測し、マイク・ペン実機・実Codexの未検証は明記する。

## 境界
常に：既存データを保持、境界検査、変更ごとの試験、作業記録更新。
別途確認：破壊的移行、外部サービス導入、利用者データへの破壊的操作。
禁止：APIキーへの切替、未取得値の補完表示、無断教材登録、検証していない成功の主張。

## リスク
CLIの音声/画像能力はテキスト生成成功から推測しない。未対応なら明示して文字入力は維持する。
旧版は追加フィールドを保持しない可能性があるため、更新後データを旧版で再保存しない。
OS電源断・音声認識品質・Windowsペンの物理操作は単体テストでは実証できない。

タスクの進捗は `tasks/todo.md` に記録する。
